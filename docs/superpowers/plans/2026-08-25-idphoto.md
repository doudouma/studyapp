# idphoto 证件照工具集成 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `template/证件照工具.html` 集成为 `/idphoto` 免费工具页面（逻辑移植为 TS 模块 + React UI 重写 + 全 5 语言 i18n + SEO 全套接入）。

**Architecture:** 核心逻辑拆为 `app/lib/idphoto/` 5 个纯 TS 模块（specs 数据 / segmentation 抠图 / face 人脸检测 / compose 合成渲染 / exportImage 导出排版），UI 为 `app/components/idphoto/` 10 个组件，状态集中在 `IdPhotoWorkbench`（useState/useMemo/useRef，无外部状态库）。AI 库运行时 CDN 动态加载（多源容错），主 bundle 零增量。

**Tech Stack:** TanStack Start + React 19 + TS、Tailwind v4、shadcn/ui（button/card/input/tabs）、face-api.js@0.22.2 与 @huggingface/transformers@3.8.1 运行时 CDN、Playwright。

**设计文档:** `docs/superpowers/specs/2026-08-25-idphoto-design.md`

**源模板对照表**（移植时按行号取材）：

| 内容 | template/证件照工具.html 行号 |
|---|---|
| SIZE_PRESETS / BG_COLORS / PAPERS / DIGITAL / headRange / headTarget | 286–373 |
| cleanMask / estimateBg / unmixBg / decontaminate | 600–760 |
| importTransformers / segmentImage | 762–845 |
| runAI 流程 | 849–916 |
| computeBase / render / srcToCanvas / drawRuler / updateCompliance | 918–1077 |
| canvasToBlobLimit / downloadBlob / 导出 | 1080–1128 |
| 相纸排版 | 1130–1186 |

**约定：**
- 每个 Task 结束时 `git commit`（执行本计划前用户已确认提交授权；禁止 push/deploy）。
- 项目无单元测试框架，验证手段 = `npx tsc --noEmit` 类型检查 + Playwright 冒烟测试 + 手测清单。
- routeTree.gen.ts 由 Vite 插件在 dev/build 时自动再生成，不要手改。

---

### Task 1: specs.ts 规格数据模块

**Files:**
- Create: `app/lib/idphoto/specs.ts`

- [ ] **Step 1: 创建 specs.ts**

```ts
// 证件照规格与常量。移植自 template/证件照工具.html L286-373。
// 名称/描述等文案不在此文件，通过 i18n key `idphoto.spec.<key>.name|.desc` 提供。

export interface Box {
  x: number;
  y: number;
  w: number;
  h: number;
}

export type DigitalKey = "DS160" | "US_RENEW" | "UK" | "ETIAS" | "JP" | "IN" | "CA";

export interface SizePreset {
  /** i18n key 后缀 */
  key: string;
  /** 国家/地区筛选值，对应 REGIONS.value */
  group: string;
  flag: string;
  w: number;
  h: number;
  wmm: number;
  hmm: number;
  /** 头高允许范围 mm（下巴→头顶含发型）；缺省按 hmm 的 55%~72% */
  headMin?: number;
  headMax?: number;
  bgDefault?: string;
  bgOptions?: string[];
  digitalKey?: DigitalKey;
  /** 背景合规提示 key：idphoto.bgNote.<key> */
  bgNoteKey?: string;
}

export const DPI = 300;
export const mm2px = (mm: number) => Math.round((mm / 25.4) * DPI);

export const SIZE_PRESETS: SizePreset[] = [
  // ===== 中国 =====
  { key: "oneInch", group: "cn", flag: "🇨🇳", w: 295, h: 413, wmm: 24.98, hmm: 34.97 },
  { key: "twoInch", group: "cn", flag: "🇨🇳", w: 413, h: 579, wmm: 34.97, hmm: 49.02 },
  { key: "smallOneInch", group: "cn", flag: "🇨🇳", w: 260, h: 378, wmm: 22.01, hmm: 32.0 },
  { key: "smallTwoInch", group: "cn", flag: "🇨🇳", w: 413, h: 531, wmm: 34.97, hmm: 44.96 },
  { key: "bigOneInch", group: "cn", flag: "🇨🇳", w: 390, h: 567, wmm: 33.02, hmm: 48.01 },
  { key: "bigTwoInch", group: "cn", flag: "🇨🇳", w: 413, h: 626, wmm: 34.97, hmm: 53.0 },
  { key: "teacherCert", group: "cn", flag: "🇨🇳", w: 295, h: 413, wmm: 24.98, hmm: 34.97 },
  { key: "civilServant", group: "cn", flag: "🇨🇳", w: 295, h: 413, wmm: 24.98, hmm: 34.97 },
  { key: "accounting", group: "cn", flag: "🇨🇳", w: 295, h: 413, wmm: 24.98, hmm: 34.97 },
  { key: "cet", group: "cn", flag: "🇨🇳", w: 144, h: 192, wmm: 12.19, hmm: 16.26 },
  { key: "ncre", group: "cn", flag: "🇨🇳", w: 390, h: 567, wmm: 33.02, hmm: 48.01 },
  { key: "postgrad", group: "cn", flag: "🇨🇳", w: 531, h: 709, wmm: 44.96, hmm: 60.03 },
  { key: "socialSecurity", group: "cn", flag: "🇨🇳", w: 358, h: 441, wmm: 30.31, hmm: 37.34 },
  { key: "eDriverLicense", group: "cn", flag: "🇨🇳", w: 260, h: 378, wmm: 22.01, hmm: 32.0 },
  { key: "fiveInch", group: "cn", flag: "🇨🇳", w: 1050, h: 1499, wmm: 88.9, hmm: 126.92 },
  // ===== 国际/签证 =====
  { key: "us2x2", group: "us", flag: "🇺🇸", w: 602, h: 602, wmm: 51, hmm: 51,
    headMin: 25, headMax: 35, bgDefault: "#FFFFFF", bgOptions: ["#FFFFFF", "#FAF9F6"],
    digitalKey: "DS160", bgNoteKey: "us" },
  { key: "ca50x70", group: "ca", flag: "🇨🇦", w: 591, h: 827, wmm: 50, hmm: 70,
    headMin: 31, headMax: 36, bgDefault: "#FFFFFF", bgOptions: ["#FFFFFF", "#D3D3D3"],
    digitalKey: "CA" },
  { key: "uk35x45", group: "uk", flag: "🇬🇧", w: 413, h: 531, wmm: 35, hmm: 45,
    headMin: 29, headMax: 34, bgDefault: "#D3D3D3", bgOptions: ["#D3D3D3", "#F5F5DC", "#FFFFFF"],
    digitalKey: "UK" },
  { key: "eu35x45", group: "eu", flag: "🇪🇺", w: 413, h: 531, wmm: 35, hmm: 45,
    headMin: 32, headMax: 36, bgDefault: "#D3D3D3", bgOptions: ["#D3D3D3", "#FFFFFF"],
    digitalKey: "ETIAS" },
  { key: "in35x45", group: "in", flag: "🇮🇳", w: 413, h: 531, wmm: 35, hmm: 45,
    headMin: 31.5, headMax: 36, bgDefault: "#FFFFFF", bgOptions: ["#FFFFFF"],
    digitalKey: "IN", bgNoteKey: "in" },
  { key: "au35x45", group: "au", flag: "🇦🇺", w: 413, h: 531, wmm: 35, hmm: 45,
    headMin: 32, headMax: 36, bgDefault: "#FFFFFF", bgOptions: ["#FFFFFF", "#D3D3D3"] },
  { key: "jp35x45", group: "jp", flag: "🇯🇵", w: 413, h: 531, wmm: 35, hmm: 45,
    headMin: 32, headMax: 36, bgDefault: "#FFFFFF", bgOptions: ["#FFFFFF", "#87CEFA"],
    digitalKey: "JP" },
  { key: "kr35x45", group: "kr", flag: "🇰🇷", w: 413, h: 531, wmm: 35, hmm: 45,
    headMin: 32, headMax: 36, bgDefault: "#FFFFFF", bgOptions: ["#FFFFFF"], bgNoteKey: "kr" },
  { key: "br50x70", group: "br", flag: "🇧🇷", w: 591, h: 827, wmm: 50, hmm: 70,
    headMin: 32, headMax: 36, bgDefault: "#FFFFFF", bgOptions: ["#FFFFFF"] },
  // ===== 自定义 =====
  { key: "custom", group: "other", flag: "🌐", w: 0, h: 0, wmm: 0, hmm: 0 },
];

export interface EffectiveSize {
  key: string;
  w: number;
  h: number;
  wmm: number;
  hmm: number;
}

export function currentSize(presetIdx: number, customW: number, customH: number): EffectiveSize {
  const p = SIZE_PRESETS[presetIdx] ?? SIZE_PRESETS[0];
  if (p.key === "custom") {
    const w = Math.max(1, customW || 295);
    const h = Math.max(1, customH || 413);
    return { key: "custom", w, h, wmm: (w / DPI) * 25.4, hmm: (h / DPI) * 25.4 };
  }
  return { key: p.key, w: p.w, h: p.h, wmm: p.wmm, hmm: p.hmm };
}

/** 头身比允许区间（占画幅高度的比例） */
export function headRange(s: Pick<SizePreset, "headMin" | "headMax" | "hmm">): [number, number] {
  let min = s.headMin;
  let max = s.headMax;
  if (!min || !max) {
    min = s.hmm * 0.55;
    max = s.hmm * 0.72;
  }
  return [min / s.hmm, max / s.hmm];
}

/** 自动定位目标比例（允许区间中点） */
export function headTarget(s: Pick<SizePreset, "headMin" | "headMax" | "hmm">): number {
  const [lo, hi] = headRange(s);
  return (lo + hi) / 2;
}

/** 国家/地区筛选项（label 用 idphoto.region.<value>） */
export const REGIONS = [
  { value: "all", flag: "" },
  { value: "cn", flag: "🇨🇳" },
  { value: "us", flag: "🇺🇸" },
  { value: "ca", flag: "🇨🇦" },
  { value: "uk", flag: "🇬🇧" },
  { value: "eu", flag: "🇪🇺" },
  { value: "in", flag: "🇮🇳" },
  { value: "au", flag: "🇦🇺" },
  { value: "jp", flag: "🇯🇵" },
  { value: "kr", flag: "🇰🇷" },
  { value: "other", flag: "🌐" },
] as const;

/** 底色色板（name 用 idphoto.bg.<key>） */
export const BG_COLORS = [
  { key: "white", value: "#FFFFFF" },
  { key: "red", value: "#FF0000" },
  { key: "blue", value: "#438EDB" },
  { key: "navyBlue", value: "#2D3A8C" },
  { key: "lightGray", value: "#D3D3D3" },
  { key: "beige", value: "#F5F5DC" },
  { key: "lightBlue", value: "#87CEFA" },
] as const;

export const PAPERS: Record<string, { wmm: number; hmm: number }> = {
  "4R": { wmm: 102, hmm: 152 },
  A6: { wmm: 105, hmm: 148 },
  A5: { wmm: 148, hmm: 210 },
  A4: { wmm: 210, hmm: 297 },
};

/** 数字提交预设（label/note 用 idphoto.digital.<k> / idphoto.digital.note.<k>） */
export const DIGITAL: Record<DigitalKey, { maxKB: number }> = {
  DS160: { maxKB: 240 },
  US_RENEW: { maxKB: 5120 },
  UK: { maxKB: 10240 },
  ETIAS: { maxKB: 10240 },
  JP: { maxKB: 600 },
  IN: { maxKB: 30 },
  CA: { maxKB: 0 },
};
```

注：巴西 group 用 `"br"`（REGIONS 无 br 项时筛选「全部」仍可见；如需单独筛选可在 REGIONS 补 `{ value: "br", flag: "🇧🇷" }` 与 `idphoto.region.br`——本计划默认补上这两处）。

- [ ] **Step 2: 在 REGIONS 数组 `kr` 之后补充 `{ value: "br", flag: "🇧🇷" }`**

- [ ] **Step 3: 类型检查**

Run: `npx tsc --noEmit`
Expected: 新文件无报错（存量警告忽略，只要没有指向 `app/lib/idphoto/specs.ts` 的错误）

- [ ] **Step 4: Commit**

```bash
git add app/lib/idphoto/specs.ts
git commit -m "feat(idphoto): 证件规格数据模块"
```

---

### Task 2: face.ts 人脸检测 + segmentation.ts AI 抠图

**Files:**
- Create: `app/lib/idphoto/face.ts`
- Create: `app/lib/idphoto/segmentation.ts`

- [ ] **Step 1: 创建 face.ts**

```ts
// face-api.js 运行时 CDN 加载 + TinyFaceDetector 人脸检测。
// 移植自 template/证件照工具.html L264-280（库加载）与 L572-593、L857-871（权重加载与检测）。
import type { Box } from "./specs";

const LIB_URLS = [
  "https://cdn.jsdelivr.net/npm/face-api.js@0.22.2/dist/face-api.min.js",
  "https://fastly.jsdelivr.net/npm/face-api.js@0.22.2/dist/face-api.min.js",
  "https://gcore.jsdelivr.net/npm/face-api.js@0.22.2/dist/face-api.min.js",
  "https://unpkg.com/face-api.js@0.22.2/dist/face-api.min.js",
];

const WEIGHT_URLS = [
  "https://cdn.jsdelivr.net/gh/justadudewhohacks/face-api.js@master/weights",
  "https://fastly.jsdelivr.net/gh/justadudewhohacks/face-api.js@master/weights",
  "https://gcore.jsdelivr.net/gh/justadudewhohacks/face-api.js@master/weights",
  "https://raw.githubusercontent.com/justadudewhohacks/face-api.js/master/weights",
];

let libPromise: Promise<boolean> | null = null;
let weightsReady = false;

function getFaceApi(): any {
  return (window as unknown as { faceapi?: unknown }).faceapi;
}

/** 注入 face-api.js script 标签，多 CDN 容错；SSR 环境返回 false */
export function loadFaceApi(): Promise<boolean> {
  if (typeof window === "undefined") return Promise.resolve(false);
  if (getFaceApi()) return Promise.resolve(true);
  libPromise ??= new Promise<boolean>((resolve) => {
    const tryLoad = (i: number) => {
      if (i >= LIB_URLS.length) {
        console.warn("face-api.js 所有 CDN 源均加载失败，将跳过人脸检测");
        resolve(false);
        return;
      }
      const s = document.createElement("script");
      s.src = LIB_URLS[i];
      s.onload = () => resolve(true);
      s.onerror = () => tryLoad(i + 1);
      document.head.appendChild(s);
    };
    tryLoad(0);
  });
  return libPromise;
}

/**
 * 检测单张人脸。库/权重任一环节不可用或未检测到人脸时返回 null，
 * 调用方应回退到居中裁剪。永不抛错。
 */
export async function detectFace(img: HTMLImageElement): Promise<Box | null> {
  try {
    const ok = await loadFaceApi();
    const faceapi = ok ? getFaceApi() : null;
    if (!faceapi) return null;
    if (!weightsReady) {
      let loaded = false;
      for (const url of WEIGHT_URLS) {
        try {
          await faceapi.nets.tinyFaceDetector.loadFromUri(url);
          loaded = true;
          break;
        } catch (e) {
          console.warn("人脸模型加载失败，换源重试", url, e);
        }
      }
      if (!loaded) return null;
      weightsReady = true;
    }
    const det = await faceapi.detectSingleFace(
      img,
      new faceapi.TinyFaceDetectorOptions({ inputSize: 512, scoreThreshold: 0.3 }),
    );
    if (!det) return null;
    const b = det.box;
    return { x: b.x, y: b.y, w: b.width, h: b.height };
  } catch (e) {
    console.warn("人脸检测异常，回退居中裁剪", e);
    return null;
  }
}
```

- [ ] **Step 2: 创建 segmentation.ts**

结构如下；`cleanMask / estimateBg / unmixBg / decontaminate` 四个内部函数从模板 L600–760 **逐行原样复制**（仅把参数 `px` 类型标注为 `Uint8ClampedArray`），其余代码如下：

```ts
// transformers.js + RMBG-1.4 浏览器端抠图管线。
// 移植自 template/证件照工具.html L595-845；进度回调改为 SegEvent 结构化事件。
export type SegEvent =
  | { kind: "runtimeLoading" }
  | { kind: "modelDownloading"; pct: number }
  | { kind: "modelCompiling" }
  | { kind: "inferring" };

const TRANSFORMERS_URLS = [
  "https://cdn.jsdelivr.net/npm/@huggingface/transformers@3.8.1",
  "https://fastly.jsdelivr.net/npm/@huggingface/transformers@3.8.1",
  "https://gcore.jsdelivr.net/npm/@huggingface/transformers@3.8.1",
  "https://unpkg.com/@huggingface/transformers@3.8.1",
];

async function importTransformers(): Promise<any> {
  let lastErr: unknown = null;
  for (const u of TRANSFORMERS_URLS) {
    try {
      /* eslint-disable-next-line */ 
      return await import(/* @vite-ignore */ u);
    } catch (e) {
      lastErr = e;
      console.warn("transformers.js 加载失败，换源重试", u, e);
    }
  }
  throw lastErr ?? new Error("AI runtime load failed");
}

let segPipe: any = null;

export interface SegmentResult {
  cut: HTMLImageElement;
  personTopSrc: number | null;
}

export async function segmentImage(
  img: HTMLImageElement,
  onEvent?: (e: SegEvent) => void,
): Promise<SegmentResult> {
  onEvent?.({ kind: "runtimeLoading" });
  const T = await importTransformers();
  T.env.allowLocalModels = false;
  if (!segPipe) {
    const progress = (p: any) => {
      if (p.status === "progress" && p.total)
        onEvent?.({ kind: "modelDownloading", pct: Math.round(((p.loaded || 0) / p.total) * 100) });
      else if (p.status === "done") onEvent?.({ kind: "modelCompiling" });
      else if (p.status === "initiate") onEvent?.({ kind: "runtimeLoading" });
    };
    // RMBG-1.4：hf-mirror 国内可达优先，失败换 huggingface.co；fp16 失败回退 q8
    const HOSTS = ["https://hf-mirror.com", "https://huggingface.co"];
    const DTYPES = ["fp16", "q8"];
    let lastErr: unknown = null;
    outer: for (const host of HOSTS) {
      T.env.remoteHost = host;
      for (const dtype of DTYPES) {
        try {
          segPipe = await T.pipeline("background-removal", "briaai/RMBG-1.4", { dtype, progress_callback: progress });
          break outer;
        } catch (e) {
          lastErr = e;
          console.warn(`抠图模型加载失败（${host} / ${dtype}），换源重试`, e);
        }
      }
    }
    if (!segPipe) throw lastErr ?? new Error("segmentation model load failed");
  }
  onEvent?.({ kind: "inferring" });

  // 超大照片先降采样到最长边 1600 再推理（模板 L809-824 原样移植）
  const MAX_SEG = 1600;
  let sw = img.naturalWidth, sh = img.naturalHeight;
  if (Math.max(sw, sh) > MAX_SEG) {
    const k = MAX_SEG / Math.max(sw, sh);
    sw = Math.round(sw * k); sh = Math.round(sh * k);
  }
  const c = document.createElement("canvas");
  c.width = sw; c.height = sh;
  const ctx0 = c.getContext("2d")!;
  ctx0.drawImage(img, 0, 0, sw, sh);
  const srcPx = ctx0.getImageData(0, 0, sw, sh).data;
  const bg = estimateBg(srcPx, sw, sh);

  const out = await segPipe(c.toDataURL("image/png"));
  const rgba = out[0]; // RawImage RGBA，与送入尺寸一致
  const px = new Uint8ClampedArray(rgba.data);
  const rw = rgba.width, rh = rgba.height;
  cleanMask(px, rw, rh);
  unmixBg(px, rw, rh, bg);
  decontaminate(px, rw, rh, bg);

  const cc = document.createElement("canvas");
  cc.width = rw; cc.height = rh;
  cc.getContext("2d")!.putImageData(new ImageData(px, rw, rh), 0, 0);

  // 从 alpha 轮廓找人物真实头顶行（模板 L834-843 原样移植）
  let topRow = -1;
  outer: for (let y = 0; y < rgba.height; y++) {
    for (let x = 0; x < rgba.width; x++) {
      if (px[(y * rgba.width + x) * 4 + 3] > 30) { topRow = y; break outer; }
    }
  }
  const cut = new Image();
  await new Promise<void>((res, rej) => { cut.onload = () => res(); cut.onerror = () => rej(new Error("cut image decode failed")); cut.src = cc.toDataURL("image/png"); });
  const personTopSrc = topRow >= 0 ? topRow * (img.naturalHeight / rgba.height) : null;
  return { cut, personTopSrc };
}

// ===== 内部函数：以下四个从 template/证件照工具.html 原样移植 =====
// cleanMask(px: Uint8ClampedArray, rw: number, rh: number)   ← L600-701
// estimateBg(px: Uint8ClampedArray, rw: number, rh: number): [number,number,number] ← L704-716
// unmixBg(px: Uint8ClampedArray, rw: number, rh: number, bg: [number,number,number]) ← L720-731
// decontaminate(px: Uint8ClampedArray, rw: number, rh: number, bg: [number,number,number]) ← L735-760
// 不加 export；estimateBg 返回值模板为数组字面量，保持一致。
```

注意两个 `outer:` 标签分别作用于各自函数内的嵌套循环，不要混用。

- [ ] **Step 3: 类型检查**

Run: `npx tsc --noEmit`
Expected: 无指向 `app/lib/idphoto/*` 的错误

- [ ] **Step 4: Commit**

```bash
git add app/lib/idphoto/face.ts app/lib/idphoto/segmentation.ts
git commit -m "feat(idphoto): AI 抠图与人脸检测模块"
```

---

### Task 3: compose.ts 合成渲染 + exportImage.ts 导出排版

**Files:**
- Create: `app/lib/idphoto/compose.ts`
- Create: `app/lib/idphoto/exportImage.ts`

- [ ] **Step 1: 创建 compose.ts**

```ts
// 自动定位、合成绘制、坐标换算、头身比标尺。移植自 template/证件照工具.html L918-1062。
import { headRange, headTarget, type Box } from "./specs";

export interface Base {
  scale: number;
  x: number;
  y: number;
}

export interface HeadSrc {
  cx: number;
  crownY: number;
  chinY: number;
}

/**
 * 计算人物在画布上的基础摆放。
 * 有脸框：按目标头身比缩放、人脸水平居中、头顶留 7% 上边距；
 * 无脸框：cover 式居中。
 */
export function computeBase(o: {
  srcW: number;
  srcH: number;
  W: number;
  H: number;
  faceBox: Box | null;
  personTop: number | null;
  target: number;
}): { base: Base; headSrc: HeadSrc | null } {
  const cover = Math.max(o.W / o.srcW, o.H / o.srcH); // 铺满画布所需最小缩放
  if (!o.faceBox) {
    return {
      base: { scale: cover, x: (o.W - o.srcW * cover) / 2, y: (o.H - o.srcH * cover) / 2 },
      headSrc: null,
    };
  }
  const f = o.faceBox;
  // 头顶优先用抠图轮廓真实顶（须位于检测框中点以上，否则视为无效回退估算）
  let crownY = f.y - f.h * 0.6;
  if (o.personTop != null && o.personTop < f.y + f.h * 0.5) crownY = o.personTop;
  const chinY = f.y + f.h;
  const headH = Math.max(1, chinY - crownY);
  let scale = (o.H * o.target) / headH;
  const faceCx = f.x + f.w / 2;
  const needX = Math.max(
    o.W / (2 * Math.max(1, faceCx)),
    o.W / (2 * Math.max(1, o.srcW - faceCx)),
  );
  scale = Math.max(scale, cover, needX);
  let y = o.H * 0.07 - crownY * scale;
  y = Math.max(y, o.H - o.srcH * scale);
  y = Math.min(y, o.H * 0.15);
  const x = o.W / 2 - faceCx * scale;
  return { base: { scale, x, y }, headSrc: { cx: faceCx, crownY, chinY } };
}

interface ComposeGeom {
  srcW: number;
  srcH: number;
  W: number;
  H: number;
  base: Base;
  zoom: number; // 百分制，100 = 1x
  adjX: number; // -100..100
  adjY: number;
}

/** 把微调后的合成结果画到结果画布（背景填充由调用方负责） */
export function renderCompose(
  ctx: CanvasRenderingContext2D,
  o: ComposeGeom & { img: CanvasImageSource },
) {
  const sc = o.base.scale * (o.zoom / 100);
  const dx = (o.adjX / 100) * o.W * 0.5;
  const dy = (o.adjY / 100) * o.H * 0.5;
  const dw = o.srcW * sc;
  const dh = o.srcH * sc;
  const cx = o.base.x + o.srcW * o.base.scale * 0.5;
  const cy = o.base.y + o.srcH * o.base.scale * 0.5;
  ctx.drawImage(o.img, cx - dw / 2 + dx, cy - dh / 2 + dy, dw, dh);
}

/** 源图坐标 → 结果画布坐标（与 renderCompose 变换一致） */
export function srcToCanvas(sx: number, sy: number, o: ComposeGeom): [number, number] {
  const sc = o.base.scale * (o.zoom / 100);
  const dx = (o.adjX / 100) * o.W * 0.5;
  const dy = (o.adjY / 100) * o.H * 0.5;
  const cx = o.base.x + o.srcW * o.base.scale * 0.5;
  const cy = o.base.y + o.srcH * o.base.scale * 0.5;
  return [cx - (o.srcW * sc) / 2 + dx + sx * sc, cy - (o.srcH * sc) / 2 + dy + sy * sc];
}

/** 头高占画幅高度的比例 */
export function complianceRatio(o: { headSrc: HeadSrc; base: Base; zoom: number; H: number }): number {
  return ((o.headSrc.chinY - o.headSrc.crownY) * o.base.scale * (o.zoom / 100)) / o.H;
}

/**
 * 在 overlay 画布上画头身比参考线与占比标签。
 * label 为调用方已翻译好的文案（如「头占比 62%」）；仅预览层，不入导出图。
 */
export function drawRuler(
  ctx: CanvasRenderingContext2D,
  o: ComposeGeom & { headSrc: HeadSrc; lo: number; hi: number; label: string },
) {
  ctx.clearRect(0, 0, o.W, o.H);
  const ratio = complianceRatio({ headSrc: o.headSrc, base: o.base, zoom: o.zoom, H: o.H });
  const [cyTopX, cyTop] = srcToCanvas(o.headSrc.cx, o.headSrc.crownY, o);
  const [, cyBot] = srcToCanvas(o.headSrc.cx, o.headSrc.chinY, o);
  const pass = ratio >= o.lo - 0.005 && ratio <= o.hi + 0.005;
  const color = pass ? "#10b981" : "#ef4444";
  const mx = o.W * 0.5;
  ctx.save();
  ctx.strokeStyle = color;
  ctx.setLineDash([7, 5]);
  ctx.lineWidth = 2;
  ctx.beginPath(); ctx.moveTo(0, cyTop); ctx.lineTo(o.W, cyTop); ctx.stroke();
  ctx.beginPath(); ctx.moveTo(0, cyBot); ctx.lineTo(o.W, cyBot); ctx.stroke();
  ctx.setLineDash([]);
  ctx.beginPath(); ctx.moveTo(mx - 18, cyTop); ctx.lineTo(mx + 18, cyTop); ctx.stroke();
  ctx.beginPath(); ctx.moveTo(mx - 18, cyBot); ctx.lineTo(mx + 18, cyBot); ctx.stroke();
  ctx.beginPath(); ctx.moveTo(mx, cyTop + 6); ctx.lineTo(mx, cyBot - 6); ctx.stroke();
  const tw = ctx.measureText(o.label).width + 14;
  let ly = cyTop - 24;
  if (ly < 4) ly = cyBot + 8;
  ctx.fillStyle = color;
  ctx.fillRect(6, ly, tw, 22);
  ctx.fillStyle = "#fff";
  ctx.font = "bold 15px sans-serif";
  ctx.textBaseline = "middle";
  ctx.fillText(o.label, 13, ly + 11);
  ctx.restore();
}

// re-export 供 Workbench 统一取用
export { headRange, headTarget };
```

注：`drawRuler` 里 `ctx.font` 需在 `measureText` 之前设置——把 `ctx.font = "bold 15px sans-serif"` 移到 `measureText` 一行之前（模板顺序有误但影响极小，此处顺手修正）。

- [ ] **Step 2: 创建 exportImage.ts**

```ts
// 导出压缩与相纸排版网格。移植自 template/证件照工具.html L1080-1186。
import { DPI, mm2px } from "./specs";

/** 按质量递减搜索把 canvas 压到 maxKB 以内（PNG 或不限时一次导出） */
export function canvasToBlobLimit(
  canvas: HTMLCanvasElement,
  format: "jpeg" | "png",
  maxKB: number,
): Promise<{ blob: Blob; note: string }> {
  return new Promise((resolve) => {
    const mime = format === "png" ? "image/png" : "image/jpeg";
    if (format === "png" || !maxKB || maxKB <= 0) {
      canvas.toBlob((b) => resolve({ blob: b!, note: "" }), mime, 0.95);
      return;
    }
    let q = 0.92;
    const tryQ = () => {
      canvas.toBlob(
        (b) => {
          const kb = b!.size / 1024;
          if (kb <= maxKB || q <= 0.1) {
            resolve({
              blob: b!,
              note: kb > maxKB ? `(${Math.round(kb)}KB)` : `(${Math.round(q * 100)}%, ${Math.round(kb)}KB)`,
            });
          } else {
            q -= 0.08;
            tryQ();
          }
        },
        mime,
        q,
      );
    };
    tryQ();
  });
}

export function downloadBlob(blob: Blob, filename: string) {
  const a = document.createElement("a");
  a.href = URL.createObjectURL(blob);
  a.download = filename;
  a.click();
  setTimeout(() => URL.revokeObjectURL(a.href), 5000);
}

export interface PrintLayout {
  cols: number;
  rows: number;
  total: number;
  paperW: number;
  paperH: number;
  photoW: number;
  photoH: number;
  startX: number;
  startY: number;
}

const GAP_MM = 2;
const MARGIN_MM = 3;

/** 计算 n 列 × m 行排版；放不下返回 null */
export function buildPrintLayout(paperWmm: number, paperHmm: number, fwmm: number, fhmm: number): PrintLayout | null {
  const paperW = mm2px(paperWmm);
  const paperH = mm2px(paperHmm);
  const photoW = mm2px(fwmm);
  const photoH = mm2px(fhmm);
  const gap = mm2px(GAP_MM);
  const margin = mm2px(MARGIN_MM);
  const cols = Math.floor((paperW - margin * 2 + gap) / (photoW + gap));
  const rows = Math.floor((paperH - margin * 2 + gap) / (photoH + gap));
  if (cols < 1 || rows < 1) return null;
  const usedW = cols * photoW + (cols - 1) * gap;
  const usedH = rows * photoH + (rows - 1) * gap;
  return {
    cols,
    rows,
    total: cols * rows,
    paperW,
    paperH,
    photoW,
    photoH,
    startX: (paperW - usedW) / 2,
    startY: (paperH - usedH) / 2,
  };
}

/** 把结果照片平铺到排版画布（300DPI），带裁剪参考线 */
export function drawPrintLayout(canvas: HTMLCanvasElement, src: HTMLCanvasElement, layout: PrintLayout, rotatePhoto: boolean) {
  canvas.width = layout.paperW;
  canvas.height = layout.paperH;
  const ctx = canvas.getContext("2d");
  if (!ctx) return;
  ctx.fillStyle = "#FFFFFF";
  ctx.fillRect(0, 0, layout.paperW, layout.paperH);
  for (let r = 0; r < layout.rows; r++) {
    for (let c = 0; c < layout.cols; c++) {
      const px = layout.startX + c * (layout.photoW + mm2px(GAP_MM));
      const py = layout.startY + r * (layout.photoH + mm2px(GAP_MM));
      if (rotatePhoto) {
        ctx.save();
        ctx.translate(px + layout.photoW / 2, py + layout.photoH / 2);
        ctx.rotate(Math.PI / 2);
        ctx.drawImage(src, -layout.photoH / 2, -layout.photoW / 2, layout.photoH, layout.photoW);
        ctx.restore();
      } else {
        ctx.drawImage(src, px, py, layout.photoW, layout.photoH);
      }
      ctx.strokeStyle = "#CCCCCC";
      ctx.lineWidth = 2;
      ctx.strokeRect(px, py, layout.photoW, layout.photoH);
    }
  }
}

export { DPI };
```

- [ ] **Step 3: 类型检查**

Run: `npx tsc --noEmit`
Expected: 通过

- [ ] **Step 4: Commit**

```bash
git add app/lib/idphoto/compose.ts app/lib/idphoto/exportImage.ts
git commit -m "feat(idphoto): 合成渲染与导出排版模块"
```

---

### Task 4: i18n 文案（5 语言全部 key）

**Files:**
- Modify: `app/lib/locales/zh.json`
- Modify: `app/lib/locales/en.json`
- Modify: `app/lib/locales/fr.json`
- Modify: `app/lib/locales/es.json`
- Modify: `app/lib/locales/pt.json`

插入位置（5 个文件一致）：
- `"nav.idphoto"` → 插在 `"nav.any2md"` 行之后
- `"freetool.item.idphoto.*"` → 插在 `"freetool.item.any2md.desc"` 行之后
- 其余 `"idphoto.*"` → 插在 `"freetool.title"` 行之前（整块）

key 总数约 135 个，5 个文件的 key 集合必须完全一致（含插值变量名）。以下给出各语言完整值。

- [ ] **Step 1: zh.json 新增块**

```json
  "nav.idphoto": "证件照工具",
  "freetool.item.idphoto.title": "AI 证件照工具",
  "freetool.item.idphoto.desc": "AI 抠图换底色，24 种规格一键生成合规证件照，支持相纸排版打印。照片不上传，浏览器本地完成。",
  "idphoto.title": "AI 证件照工具 - 尺寸修改 / 换底色 / 排版打印 | 100mini",
  "idphoto.desc": "免费在线证件照工具：AI 抠图换底色、24 种中外规格尺寸修改、头身比合规检测、文件大小控制、相纸排版打印。全部浏览器本地完成，照片不上传。",
  "idphoto.keywords": "证件照,证件照制作,AI抠图,一寸照片,二寸照片,签证照片,换底色,照片排版打印,免费证件照",
  "idphoto.heading": "AI 证件照工具",
  "idphoto.subheading": "尺寸修改 · AI 换底色 · 文件大小控制 · 相纸排版打印 —— 全部本地完成，照片不上传",
  "idphoto.step.open": "打开照片",
  "idphoto.step.spec": "选择证件规格",
  "idphoto.step.bg": "选择证件底色",
  "idphoto.step.ai": "AI 生成证件照",
  "idphoto.step.compliance": "合规检测与头身比标尺",
  "idphoto.step.adjust": "手动微调（所见即所得）",
  "idphoto.step.export": "导出证件照",
  "idphoto.drop.main": "点击或拖拽照片到这里",
  "idphoto.drop.sub": "支持 JPG / PNG / WEBP ｜ 建议相对正面照，手机自拍即可",
  "idphoto.drop.selected": "已选择：{{name}}",
  "idphoto.drop.errType": "请选择图片文件。",
  "idphoto.drop.errLoad": "图片加载失败，请换一张试试。",
  "idphoto.region.all": "🌐 全部规格",
  "idphoto.region.cn": "🇨🇳 中国",
  "idphoto.region.us": "🇺🇸 美国",
  "idphoto.region.ca": "🇨🇦 加拿大",
  "idphoto.region.uk": "🇬🇧 英国",
  "idphoto.region.eu": "🇪🇺 申根/EU",
  "idphoto.region.in": "🇮🇳 印度",
  "idphoto.region.au": "🇦🇺 澳洲",
  "idphoto.region.jp": "🇯🇵 日本",
  "idphoto.region.kr": "🇰🇷 韩国",
  "idphoto.region.br": "🇧🇷 巴西",
  "idphoto.region.other": "🔧 自定义",
  "idphoto.spec.oneInch.name": "一寸", "idphoto.spec.oneInch.desc": "简历、学生证常用",
  "idphoto.spec.twoInch.name": "二寸", "idphoto.spec.twoInch.desc": "毕业证、简历常用",
  "idphoto.spec.smallOneInch.name": "小一寸", "idphoto.spec.smallOneInch.desc": "驾驶证、体检表",
  "idphoto.spec.smallTwoInch.name": "小二寸", "idphoto.spec.smallTwoInch.desc": "护照、签证常用",
  "idphoto.spec.bigOneInch.name": "大一寸", "idphoto.spec.bigOneInch.desc": "部分考试报名",
  "idphoto.spec.bigTwoInch.name": "大二寸", "idphoto.spec.bigTwoInch.desc": "部分资格考试",
  "idphoto.spec.teacherCert.name": "教师资格证", "idphoto.spec.teacherCert.desc": "教资报名专用",
  "idphoto.spec.civilServant.name": "国家公务员考试", "idphoto.spec.civilServant.desc": "国考报名专用",
  "idphoto.spec.accounting.name": "初级会计考试", "idphoto.spec.accounting.desc": "会计资格报名",
  "idphoto.spec.cet.name": "英语四六级考试", "idphoto.spec.cet.desc": "四六级报名",
  "idphoto.spec.ncre.name": "计算机等级考试", "idphoto.spec.ncre.desc": "NCRE 报名",
  "idphoto.spec.postgrad.name": "研究生考试", "idphoto.spec.postgrad.desc": "考研报名",
  "idphoto.spec.socialSecurity.name": "社保卡", "idphoto.spec.socialSecurity.desc": "社保卡照片",
  "idphoto.spec.eDriverLicense.name": "电子驾驶证", "idphoto.spec.eDriverLicense.desc": "交管12123",
  "idphoto.spec.fiveInch.name": "五寸", "idphoto.spec.fiveInch.desc": "生活照打印",
  "idphoto.spec.us2x2.name": "美国 2×2", "idphoto.spec.us2x2.desc": "美国签证、护照 2×2 英寸",
  "idphoto.spec.ca50x70.name": "加拿大 50×70", "idphoto.spec.ca50x70.desc": "加拿大签证、移民 50×70mm",
  "idphoto.spec.uk35x45.name": "英国 35×45", "idphoto.spec.uk35x45.desc": "英国签证 35×45mm",
  "idphoto.spec.eu35x45.name": "申根/EU 35×45", "idphoto.spec.eu35x45.desc": "申根签证、ETIAS 35×45mm",
  "idphoto.spec.in35x45.name": "印度 35×45", "idphoto.spec.in35x45.desc": "印度签证 35×45mm",
  "idphoto.spec.au35x45.name": "澳洲 35×45", "idphoto.spec.au35x45.desc": "澳洲签证 35×45mm",
  "idphoto.spec.jp35x45.name": "日本 35×45", "idphoto.spec.jp35x45.desc": "日本签证 35×45mm",
  "idphoto.spec.kr35x45.name": "韩国 35×45", "idphoto.spec.kr35x45.desc": "韩国签证 35×45mm（仅白底）",
  "idphoto.spec.br50x70.name": "巴西 50×70", "idphoto.spec.br50x70.desc": "巴西签证 50×70mm",
  "idphoto.spec.custom.name": "自定义", "idphoto.spec.custom.desc": "自由设置宽高",
  "idphoto.custom.w": "宽(px)",
  "idphoto.custom.h": "高(px)",
  "idphoto.mmInfo": "像素 {{w}}×{{h}} ｜ 约 {{wmm}}×{{hmm}} mm（按300DPI）",
  "idphoto.bg.white": "白色",
  "idphoto.bg.red": "红色",
  "idphoto.bg.blue": "蓝色",
  "idphoto.bg.navyBlue": "深蓝",
  "idphoto.bg.lightGray": "浅灰",
  "idphoto.bg.beige": "米色",
  "idphoto.bg.lightBlue": "浅蓝",
  "idphoto.bg.keep": "保留原背景",
  "idphoto.bgNote.us": "⚠️ 美国护照数字提交不允许修改背景（国务院规定），请使用白底原图，勿用 AI 换底。",
  "idphoto.bgNote.in": "⚠️ 印度多数门户要求纯白底、双耳露出，且文件常限 20–50KB。",
  "idphoto.bgNote.kr": "⚠️ 韩国只认纯白底，奶油色/浅灰会被拒，请保持白色。",
  "idphoto.btn.ai": "一键 AI 生成（抠图 + 定位）",
  "idphoto.btn.cropOnly": "仅裁剪排版（不抠图）",
  "idphoto.ai.hint": "首次使用需联网下载 AI 模型（约 88MB，之后浏览器缓存离线可用）。也可勾选「保留原背景」后点「仅裁剪排版」跳过 AI。",
  "idphoto.status.noPhoto": "请先打开照片。",
  "idphoto.status.loaded": "已加载：{{name}}\n点击「一键 AI 生成」或「仅裁剪排版」。",
  "idphoto.status.faceModel": "正在加载人脸检测模型…",
  "idphoto.status.runtimeLoading": "正在加载 AI 组件…",
  "idphoto.status.downloading": "正在下载抠图模型… {{pct}}%",
  "idphoto.status.compiling": "模型下载完成，正在编译初始化（首次需 1~3 分钟，请耐心等待）…",
  "idphoto.status.inferring": "正在 AI 抠图去背景（约 10~30 秒）…",
  "idphoto.status.done": "生成完成！可微调位置后导出；如需打印请切换「相纸排版」标签页。",
  "idphoto.status.cropOnlyDone": "已按居中裁剪排版。可手动微调位置后导出。",
  "idphoto.status.needAiFirst": "换底色需要先抠图：请点击「一键 AI 生成」。只需抠图一次，之后切换底色即时生效。",
  "idphoto.ai.failPrefix": "AI 处理失败：",
  "idphoto.ai.netHint": "这是网络问题：模型需要联网下载。公司/校园网可能拦截了下载，建议换网络或用手机热点重试。",
  "idphoto.ai.offlineHint": "也可勾选「保留原背景」后用「仅裁剪排版」（无需联网）。",
  "idphoto.compliance.unknown": "生成后自动检测头身比",
  "idphoto.compliance.ok": "✓ 头身比合规：{{pct}}%（要求 {{lo}}–{{hi}}%）",
  "idphoto.compliance.bad": "✗ 头身比超出范围：{{pct}}%（要求 {{lo}}–{{hi}}%），可用微调修正",
  "idphoto.ruler.show": "在预览上显示头身比标尺",
  "idphoto.tip.1": "表情自然中性，不要露齿笑（多数国家硬性要求）",
  "idphoto.tip.2": "摘下眼镜，避免镜片反光/边框阴影（美、英等明确禁止）",
  "idphoto.tip.3": "光线均匀，脸部无强烈阴影，背景平整无杂物",
  "idphoto.tip.4": "双耳尽量露出（印度/部分签证要求）",
  "idphoto.tip.5": "不化浓妆、不戴头饰（宗教原因除外）",
  "idphoto.adj.zoom": "缩放",
  "idphoto.adj.x": "水平",
  "idphoto.adj.y": "垂直",
  "idphoto.adj.reset": "重置微调",
  "idphoto.digital.label": "数字提交预设",
  "idphoto.digital.none": "自定义 / 不限",
  "idphoto.digital.DS160": "美国 DS-160（<240KB）",
  "idphoto.digital.US_RENEW": "美国护照续签（<5MB）",
  "idphoto.digital.UK": "英国在线（≥600×750，<10MB）",
  "idphoto.digital.ETIAS": "申根 ETIAS（<10MB）",
  "idphoto.digital.JP": "日本 Myna（<600KB）",
  "idphoto.digital.IN": "印度门户（≤30KB）",
  "idphoto.digital.CA": "加拿大（≥420×540）",
  "idphoto.digital.note.DS160": "美国 DS-160：文件需小于 240KB，建议 600×600px 起",
  "idphoto.digital.note.US_RENEW": "美国护照续签：小于 5MB",
  "idphoto.digital.note.UK": "英国在线申请：≥600×750px，小于 10MB",
  "idphoto.digital.note.ETIAS": "申根 ETIAS：小于 10MB",
  "idphoto.digital.note.JP": "日本 MynaPortal：小于 600KB，建议 600×730px",
  "idphoto.digital.note.IN": "印度门户：常要求 ≤30KB（部分 ≤50KB）",
  "idphoto.digital.note.CA": "加拿大：≥420×540px，不限大小",
  "idphoto.export.format": "格式",
  "idphoto.export.jpg": "JPG",
  "idphoto.export.png": "PNG",
  "idphoto.export.sizeLimit": "大小上限",
  "idphoto.export.kbUnit": "KB（0=不限）",
  "idphoto.export.btn": "导出证件照",
  "idphoto.export.done": "已导出 {{info}}",
  "idphoto.tab.photo": "证件照预览",
  "idphoto.tab.print": "相纸排版打印",
  "idphoto.paper.label": "相纸",
  "idphoto.paper.4R": "6寸相纸 (102×152mm)",
  "idphoto.paper.A6": "A6 (105×148mm)",
  "idphoto.paper.A5": "A5 (148×210mm)",
  "idphoto.paper.A4": "A4 (210×297mm)",
  "idphoto.orient.portrait": "竖向",
  "idphoto.orient.landscape": "横向",
  "idphoto.print.paperOrient": "相纸方向",
  "idphoto.print.photoOrient": "照片方向",
  "idphoto.print.btnLayout": "生成排版",
  "idphoto.print.btnExportPrint": "导出排版图",
  "idphoto.print.initial": "生成证件照后，点击「生成排版」。排版按 300 DPI 输出，可直接送打印店或家用打印机打印。",
  "idphoto.print.fitError": "当前相纸放不下该规格照片，请换更大的相纸或调整方向。",
  "idphoto.print.summary": "{{paper}}｜每张 {{fw}}×{{fh}}mm｜共 {{total}} 张（{{cols}}列×{{rows}}行）｜输出 {{w}}×{{h}}px @300DPI",
  "idphoto.print.needGenerate": "请先生成证件照。",
  "idphoto.headRatio": "头占比 {{pct}}%",
  "idphoto.file.photo": "证件照",
  "idphoto.file.print": "相纸排版"
```

注意：JSON 里 `<` 直接可用；`"idphoto.digital.DS160"` 含 `<240KB` 无需转义。

- [ ] **Step 2: en.json 新增块**

```json
  "nav.idphoto": "ID Photo",
  "freetool.item.idphoto.title": "AI ID Photo Tool",
  "freetool.item.idphoto.desc": "AI background removal & color swap for 24 compliant ID photo sizes, with print sheet layout. 100% local in your browser.",
  "idphoto.title": "AI ID Photo Tool - Resize / Background / Print Layout | 100mini",
  "idphoto.desc": "Free online ID photo tool: AI background removal, 24 passport & visa sizes, head-height compliance check, file size control and print sheet layout. Everything runs locally in your browser — photos never leave your device.",
  "idphoto.keywords": "ID photo,passport photo,visa photo,AI background remover,biometric photo,photo resize,print sheet,free ID photo",
  "idphoto.heading": "AI ID Photo Tool",
  "idphoto.subheading": "Resize · AI background swap · File size control · Print layout — all local, photos never uploaded",
  "idphoto.step.open": "Open a Photo",
  "idphoto.step.spec": "Choose a Size",
  "idphoto.step.bg": "Choose a Background",
  "idphoto.step.ai": "Generate with AI",
  "idphoto.step.compliance": "Compliance Check & Head Ruler",
  "idphoto.step.adjust": "Fine-tune (WYSIWYG)",
  "idphoto.step.export": "Export",
  "idphoto.drop.main": "Click or drop a photo here",
  "idphoto.drop.sub": "JPG / PNG / WEBP · A front-facing phone selfie works fine",
  "idphoto.drop.selected": "Selected: {{name}}",
  "idphoto.drop.errType": "Please choose an image file.",
  "idphoto.drop.errLoad": "Failed to load the image. Try another one.",
  "idphoto.region.all": "🌐 All sizes",
  "idphoto.region.cn": "🇨🇳 China",
  "idphoto.region.us": "🇺🇸 USA",
  "idphoto.region.ca": "🇨🇦 Canada",
  "idphoto.region.uk": "🇬🇧 UK",
  "idphoto.region.eu": "🇪🇺 Schengen/EU",
  "idphoto.region.in": "🇮🇳 India",
  "idphoto.region.au": "🇦🇺 Australia",
  "idphoto.region.jp": "🇯🇵 Japan",
  "idphoto.region.kr": "🇰🇷 Korea",
  "idphoto.region.br": "🇧🇷 Brazil",
  "idphoto.region.other": "🔧 Custom",
  "idphoto.spec.oneInch.name": "One-inch", "idphoto.spec.oneInch.desc": "Resumes & student IDs",
  "idphoto.spec.twoInch.name": "Two-inch", "idphoto.spec.twoInch.desc": "Diplomas & resumes",
  "idphoto.spec.smallOneInch.name": "Small 1-inch", "idphoto.spec.smallOneInch.desc": "Driver's license & medical forms",
  "idphoto.spec.smallTwoInch.name": "Small 2-inch", "idphoto.spec.smallTwoInch.desc": "Passports & visas",
  "idphoto.spec.bigOneInch.name": "Large 1-inch", "idphoto.spec.bigOneInch.desc": "Some exam registrations",
  "idphoto.spec.bigTwoInch.name": "Large 2-inch", "idphoto.spec.bigTwoInch.desc": "Some qualification exams",
  "idphoto.spec.teacherCert.name": "Teacher Cert", "idphoto.spec.teacherCert.desc": "Teacher certification application",
  "idphoto.spec.civilServant.name": "Civil Service Exam", "idphoto.spec.civilServant.desc": "National civil service exam",
  "idphoto.spec.accounting.name": "Accounting Exam", "idphoto.spec.accounting.desc": "Junior accounting qualification",
  "idphoto.spec.cet.name": "CET-4/6", "idphoto.spec.cet.desc": "College English test registration",
  "idphoto.spec.ncre.name": "NCRE", "idphoto.spec.ncre.desc": "Computer rank exam registration",
  "idphoto.spec.postgrad.name": "Postgraduate Exam", "idphoto.spec.postgrad.desc": "Graduate entrance exam",
  "idphoto.spec.socialSecurity.name": "Social Security Card", "idphoto.spec.socialSecurity.desc": "Social security card photo",
  "idphoto.spec.eDriverLicense.name": "E-Driver's License", "idphoto.spec.eDriverLicense.desc": "Digital driver's license",
  "idphoto.spec.fiveInch.name": "5-inch", "idphoto.spec.fiveInch.desc": "Photo printing",
  "idphoto.spec.us2x2.name": "US 2×2", "idphoto.spec.us2x2.desc": "US visa & passport, 2×2 inches",
  "idphoto.spec.ca50x70.name": "Canada 50×70", "idphoto.spec.ca50x70.desc": "Canadian visa & immigration",
  "idphoto.spec.uk35x45.name": "UK 35×45", "idphoto.spec.uk35x45.desc": "UK visa photo",
  "idphoto.spec.eu35x45.name": "Schengen/EU 35×45", "idphoto.spec.eu35x45.desc": "Schengen visa & ETIAS",
  "idphoto.spec.in35x45.name": "India 35×45", "idphoto.spec.in35x45.desc": "India visa photo",
  "idphoto.spec.au35x45.name": "Australia 35×45", "idphoto.spec.au35x45.desc": "Australia visa photo",
  "idphoto.spec.jp35x45.name": "Japan 35×45", "idphoto.spec.jp35x45.desc": "Japan visa photo",
  "idphoto.spec.kr35x45.name": "Korea 35×45", "idphoto.spec.kr35x45.desc": "Korea visa photo (white only)",
  "idphoto.spec.br50x70.name": "Brazil 50×70", "idphoto.spec.br50x70.desc": "Brazil visa photo",
  "idphoto.spec.custom.name": "Custom", "idphoto.spec.custom.desc": "Set width & height freely",
  "idphoto.custom.w": "Width (px)",
  "idphoto.custom.h": "Height (px)",
  "idphoto.mmInfo": "{{w}}×{{h}} px · about {{wmm}}×{{hmm}} mm @300DPI",
  "idphoto.bg.white": "White",
  "idphoto.bg.red": "Red",
  "idphoto.bg.blue": "Blue",
  "idphoto.bg.navyBlue": "Navy blue",
  "idphoto.bg.lightGray": "Light gray",
  "idphoto.bg.beige": "Beige",
  "idphoto.bg.lightBlue": "Light blue",
  "idphoto.bg.keep": "Keep original background",
  "idphoto.bgNote.us": "⚠️ US digital passport submissions must keep the original white background (State Dept rule) — do not use the AI background swap.",
  "idphoto.bgNote.in": "⚠️ Most Indian portals require pure white background and visible ears; files are often limited to 20–50KB.",
  "idphoto.bgNote.kr": "⚠️ Korea only accepts pure white — cream/light gray will be rejected.",
  "idphoto.btn.ai": "AI Generate (cutout + align)",
  "idphoto.btn.cropOnly": "Crop Only (no AI)",
  "idphoto.ai.hint": "First use downloads the AI model (~88MB, cached offline afterwards). You can also check \"Keep original background\" and use \"Crop Only\" to skip AI.",
  "idphoto.status.noPhoto": "Open a photo first.",
  "idphoto.status.loaded": "Loaded: {{name}}\nClick \"AI Generate\" or \"Crop Only\".",
  "idphoto.status.faceModel": "Loading face detection model…",
  "idphoto.status.runtimeLoading": "Loading AI runtime…",
  "idphoto.status.downloading": "Downloading cutout model… {{pct}}%",
  "idphoto.status.compiling": "Model downloaded, compiling & initializing (first run takes 1–3 min)…",
  "idphoto.status.inferring": "Removing background with AI (10–30s)…",
  "idphoto.status.done": "Done! Fine-tune if needed, then export. For printing switch to the \"Print Layout\" tab.",
  "idphoto.status.cropOnlyDone": "Center-cropped. Fine-tune if needed, then export.",
  "idphoto.status.needAiFirst": "Changing the background requires a cutout first: click \"AI Generate\". Cut out once, then switching colors is instant.",
  "idphoto.ai.failPrefix": "AI processing failed: ",
  "idphoto.ai.netHint": "This looks like a network issue: the model must be downloaded online. Corporate/campus networks may block it — try another network or a mobile hotspot.",
  "idphoto.ai.offlineHint": "Alternatively check \"Keep original background\" and use \"Crop Only\" (no network needed).",
  "idphoto.compliance.unknown": "Head ratio is checked after generation",
  "idphoto.compliance.ok": "✓ Head ratio OK: {{pct}}% (required {{lo}}–{{hi}}%)",
  "idphoto.compliance.bad": "✗ Head ratio out of range: {{pct}}% (required {{lo}}–{{hi}}%). Use fine-tune to fix.",
  "idphoto.ruler.show": "Show head ruler on preview",
  "idphoto.tip.1": "Neutral expression, no smiling with teeth (mandatory in most countries)",
  "idphoto.tip.2": "Remove glasses — reflections/frames are banned (e.g. US, UK)",
  "idphoto.tip.3": "Even lighting, no harsh face shadows, plain background",
  "idphoto.tip.4": "Ears visible where possible (India/some visas)",
  "idphoto.tip.5": "No heavy makeup or headwear (religious exceptions aside)",
  "idphoto.adj.zoom": "Zoom",
  "idphoto.adj.x": "Horizontal",
  "idphoto.adj.y": "Vertical",
  "idphoto.adj.reset": "Reset adjustments",
  "idphoto.digital.label": "Digital submission preset",
  "idphoto.digital.none": "Custom / unlimited",
  "idphoto.digital.DS160": "US DS-160 (<240KB)",
  "idphoto.digital.US_RENEW": "US passport renewal (<5MB)",
  "idphoto.digital.UK": "UK online (≥600×750, <10MB)",
  "idphoto.digital.ETIAS": "Schengen ETIAS (<10MB)",
  "idphoto.digital.JP": "Japan Myna (<600KB)",
  "idphoto.digital.IN": "India portals (≤30KB)",
  "idphoto.digital.CA": "Canada (≥420×540)",
  "idphoto.digital.note.DS160": "US DS-160: file must be under 240KB, start from 600×600px",
  "idphoto.digital.note.US_RENEW": "US passport renewal: under 5MB",
  "idphoto.digital.note.UK": "UK online application: ≥600×750px, under 10MB",
  "idphoto.digital.note.ETIAS": "Schengen ETIAS: under 10MB",
  "idphoto.digital.note.JP": "Japan Myna Portal: under 600KB, 600×730px recommended",
  "idphoto.digital.note.IN": "India portals: usually ≤30KB required (some ≤50KB)",
  "idphoto.digital.note.CA": "Canada: ≥420×540px, no size limit",
  "idphoto.export.format": "Format",
  "idphoto.export.jpg": "JPG",
  "idphoto.export.png": "PNG",
  "idphoto.export.sizeLimit": "Size limit",
  "idphoto.export.kbUnit": "KB (0 = unlimited)",
  "idphoto.export.btn": "Export photo",
  "idphoto.export.done": "Exported {{info}}",
  "idphoto.tab.photo": "Photo Preview",
  "idphoto.tab.print": "Print Layout",
  "idphoto.paper.label": "Paper",
  "idphoto.paper.4R": '4×6" (102×152mm)',
  "idphoto.paper.A6": "A6 (105×148mm)",
  "idphoto.paper.A5": "A5 (148×210mm)",
  "idphoto.paper.A4": "A4 (210×297mm)",
  "idphoto.orient.portrait": "Portrait",
  "idphoto.orient.landscape": "Landscape",
  "idphoto.print.paperOrient": "Paper orientation",
  "idphoto.print.photoOrient": "Photo orientation",
  "idphoto.print.btnLayout": "Build layout",
  "idphoto.print.btnExportPrint": "Export layout",
  "idphoto.print.initial": "After generating the photo, click \"Build layout\". Output is 300 DPI, ready for print shops or home printers.",
  "idphoto.print.fitError": "This paper size cannot fit the photo. Use larger paper or change orientation.",
  "idphoto.print.summary": "{{paper}} | {{fw}}×{{fh}}mm each | {{total}} photos ({{cols}} cols × {{rows}} rows) | {{w}}×{{h}}px @300DPI",
  "idphoto.print.needGenerate": "Generate the photo first.",
  "idphoto.headRatio": "Head {{pct}}%",
  "idphoto.file.photo": "id_photo",
  "idphoto.file.print": "print_layout"
```

- [ ] **Step 3: fr.json 新增块**

```json
  "nav.idphoto": "Photo d'identité",
  "freetool.item.idphoto.title": "Outil photo d'identité IA",
  "freetool.item.idphoto.desc": "Détourage IA et changement de fond pour 24 formats de photo d'identité, avec planche d'impression. 100 % local dans le navigateur.",
  "idphoto.title": "Outil photo d'identité IA - Redimensionner / Fond / Planche | 100mini",
  "idphoto.desc": "Outil gratuit de photo d'identité : détourage IA, 24 formats passeport & visa, contrôle de conformité du visage, contrôle du poids fichier et planche d'impression. Tout en local, les photos ne quittent jamais votre appareil.",
  "idphoto.keywords": "photo d'identité,photo passeport,photo visa,détourage IA,photo biométrique,redimensionner photo,planche d'impression,gratuit",
  "idphoto.heading": "Outil photo d'identité IA",
  "idphoto.subheading": "Redimension · Fond IA · Poids du fichier · Planche d'impression — tout en local, aucune photo envoyée",
  "idphoto.step.open": "Ouvrir une photo",
  "idphoto.step.spec": "Choisir le format",
  "idphoto.step.bg": "Choisir le fond",
  "idphoto.step.ai": "Générer avec l'IA",
  "idphoto.step.compliance": "Conformité & règle visage",
  "idphoto.step.adjust": "Ajustement fin (WYSIWYG)",
  "idphoto.step.export": "Exporter",
  "idphoto.drop.main": "Cliquez ou déposez une photo ici",
  "idphoto.drop.sub": "JPG / PNG / WEBP · un selfie de face suffit",
  "idphoto.drop.selected": "Sélectionné : {{name}}",
  "idphoto.drop.errType": "Veuillez choisir un fichier image.",
  "idphoto.drop.errLoad": "Échec du chargement. Essayez une autre image.",
  "idphoto.region.all": "🌐 Tous les formats",
  "idphoto.region.cn": "🇨🇳 Chine",
  "idphoto.region.us": "🇺🇸 États-Unis",
  "idphoto.region.ca": "🇨🇦 Canada",
  "idphoto.region.uk": "🇬🇧 Royaume-Uni",
  "idphoto.region.eu": "🇪🇺 Schengen/UE",
  "idphoto.region.in": "🇮🇳 Inde",
  "idphoto.region.au": "🇦🇺 Australie",
  "idphoto.region.jp": "🇯🇵 Japon",
  "idphoto.region.kr": "🇰🇷 Corée",
  "idphoto.region.br": "🇧🇷 Brésil",
  "idphoto.region.other": "🔧 Personnalisé",
  "idphoto.spec.oneInch.name": "1 pouce", "idphoto.spec.oneInch.desc": "CV & carte étudiant",
  "idphoto.spec.twoInch.name": "2 pouces", "idphoto.spec.twoInch.desc": "Diplômes & CV",
  "idphoto.spec.smallOneInch.name": "Petit 1 pouce", "idphoto.spec.smallOneInch.desc": "Permis & bilans médicaux",
  "idphoto.spec.smallTwoInch.name": "Petit 2 pouces", "idphoto.spec.smallTwoInch.desc": "Passeports & visas",
  "idphoto.spec.bigOneInch.name": "Grand 1 pouce", "idphoto.spec.bigOneInch.desc": "Inscriptions à certains examens",
  "idphoto.spec.bigTwoInch.name": "Grand 2 pouces", "idphoto.spec.bigTwoInch.desc": "Certains examens professionnels",
  "idphoto.spec.teacherCert.name": "Certif. enseignant", "idphoto.spec.teacherCert.desc": "Candidature enseignement",
  "idphoto.spec.civilServant.name": "Fonction publique", "idphoto.spec.civilServant.desc": "Concours national",
  "idphoto.spec.accounting.name": "Examen comptabilité", "idphoto.spec.accounting.desc": "Comptabilité junior",
  "idphoto.spec.cet.name": "CET-4/6", "idphoto.spec.cet.desc": "Test d'anglais universitaire",
  "idphoto.spec.ncre.name": "NCRE", "idphoto.spec.ncre.desc": "Examen informatique national",
  "idphoto.spec.postgrad.name": "Concours master", "idphoto.spec.postgrad.desc": "Admission en master",
  "idphoto.spec.socialSecurity.name": "Carte vitale", "idphoto.spec.socialSecurity.desc": "Photo sécurité sociale",
  "idphoto.spec.eDriverLicense.name": "Permis numérique", "idphoto.spec.eDriverLicense.desc": "Permis électronique",
  "idphoto.spec.fiveInch.name": "5 pouces", "idphoto.spec.fiveInch.desc": "Tirage photo",
  "idphoto.spec.us2x2.name": "USA 2×2", "idphoto.spec.us2x2.desc": "Visa & passeport US, 2×2 pouces",
  "idphoto.spec.ca50x70.name": "Canada 50×70", "idphoto.spec.ca50x70.desc": "Visa & immigration Canada",
  "idphoto.spec.uk35x45.name": "UK 35×45", "idphoto.spec.uk35x45.desc": "Photo visa Royaume-Uni",
  "idphoto.spec.eu35x45.name": "Schengen/UE 35×45", "idphoto.spec.eu35x45.desc": "Visa Schengen & ETIAS",
  "idphoto.spec.in35x45.name": "Inde 35×45", "idphoto.spec.in35x45.desc": "Photo visa Inde",
  "idphoto.spec.au35x45.name": "Australie 35×45", "idphoto.spec.au35x45.desc": "Photo visa Australie",
  "idphoto.spec.jp35x45.name": "Japon 35×45", "idphoto.spec.jp35x45.desc": "Photo visa Japon",
  "idphoto.spec.kr35x45.name": "Corée 35×45", "idphoto.spec.kr35x45.desc": "Photo visa Corée (blanc uniquement)",
  "idphoto.spec.br50x70.name": "Brésil 50×70", "idphoto.spec.br50x70.desc": "Photo visa Brésil",
  "idphoto.spec.custom.name": "Personnalisé", "idphoto.spec.custom.desc": "Largeur & hauteur libres",
  "idphoto.custom.w": "Largeur (px)",
  "idphoto.custom.h": "Hauteur (px)",
  "idphoto.mmInfo": "{{w}}×{{h}} px · env. {{wmm}}×{{hmm}} mm @300DPI",
  "idphoto.bg.white": "Blanc",
  "idphoto.bg.red": "Rouge",
  "idphoto.bg.blue": "Bleu",
  "idphoto.bg.navyBlue": "Bleu marine",
  "idphoto.bg.lightGray": "Gris clair",
  "idphoto.bg.beige": "Beige",
  "idphoto.bg.lightBlue": "Bleu clair",
  "idphoto.bg.keep": "Garder le fond d'origine",
  "idphoto.bgNote.us": "⚠️ Les demandes numériques US exigent le fond blanc d'origine (règle du Département d'État) — n'utilisez pas le remplacement IA.",
  "idphoto.bgNote.in": "⚠️ La plupart des portails indiens exigent un fond blanc pur et les oreilles visuelles ; fichiers souvent limités à 20–50 Ko.",
  "idphoto.bgNote.kr": "⚠️ La Corée n'accepte que le blanc pur — crème/gris clair refusé.",
  "idphoto.btn.ai": "Générer IA (découpe + cadrage)",
  "idphoto.btn.cropOnly": "Recadrer seulement (sans IA)",
  "idphoto.ai.hint": "La première utilisation télécharge le modèle IA (~88 Mo, mis en cache ensuite). Vous pouvez aussi cocher « Garder le fond d'origine » puis « Recadrer seulement ».",
  "idphoto.status.noPhoto": "Ouvrez d'abord une photo.",
  "idphoto.status.loaded": "Chargée : {{name}}\nCliquez sur « Générer IA » ou « Recadrer seulement ».",
  "idphoto.status.faceModel": "Chargement du modèle de détection de visage…",
  "idphoto.status.runtimeLoading": "Chargement du runtime IA…",
  "idphoto.status.downloading": "Téléchargement du modèle… {{pct}}%",
  "idphoto.status.compiling": "Modèle téléchargé, compilation en cours (1–3 min au premier lancement)…",
  "idphoto.status.inferring": "Suppression du fond par IA (10–30 s)…",
  "idphoto.status.done": "Terminé ! Ajustez si besoin puis exportez. Pour imprimer, ouvrez l'onglet « Planche ».",
  "idphoto.status.cropOnlyDone": "Recadrage central effectué. Ajustez si besoin puis exportez.",
  "idphoto.status.needAiFirst": "Changer le fond exige d'abord un détourage : cliquez « Générer IA ». Un seul détourage, ensuite les changements sont instantanés.",
  "idphoto.ai.failPrefix": "Échec du traitement IA : ",
  "idphoto.ai.netHint": "Problème réseau probable : le modèle doit être téléchargé. Les réseaux entreprise/université peuvent le bloquer — essayez un autre réseau ou un partage de connexion.",
  "idphoto.ai.offlineHint": "Sinon, cochez « Garder le fond d'origine » puis « Recadrer seulement » (sans connexion).",
  "idphoto.compliance.unknown": "Le ratio du visage sera vérifié après génération",
  "idphoto.compliance.ok": "✓ Ratio conforme : {{pct}}% (requis {{lo}}–{{hi}}%)",
  "idphoto.compliance.bad": "✗ Ratio hors limites : {{pct}}% (requis {{lo}}–{{hi}}%). Utilisez l'ajustement fin.",
  "idphoto.ruler.show": "Afficher la règle visage sur l'aperçu",
  "idphoto.tip.1": "Expression neutre, sans sourire denté (obligatoire dans la plupart des pays)",
  "idphoto.tip.2": "Retirez les lunettes — reflets/montures interdits (US, UK…)",
  "idphoto.tip.3": "Lumière uniforme, pas d'ombres marquées, fond uni",
  "idphoto.tip.4": "Oreilles visibles si possible (Inde/certains visas)",
  "idphoto.tip.5": "Pas de maquillage marqué ni couvre-chef (sauf motif religieux)",
  "idphoto.adj.zoom": "Zoom",
  "idphoto.adj.x": "Horizontal",
  "idphoto.adj.y": "Vertical",
  "idphoto.adj.reset": "Réinitialiser",
  "idphoto.digital.label": "Préréglage de dépôt numérique",
  "idphoto.digital.none": "Personnalisé / illimité",
  "idphoto.digital.DS160": "USA DS-160 (<240 Ko)",
  "idphoto.digital.US_RENEW": "Renouvellement passeport USA (<5 Mo)",
  "idphoto.digital.UK": "UK en ligne (≥600×750, <10 Mo)",
  "idphoto.digital.ETIAS": "Schengen ETIAS (<10 Mo)",
  "idphoto.digital.JP": "Japon Myna (<600 Ko)",
  "idphoto.digital.IN": "Portails Inde (≤30 Ko)",
  "idphoto.digital.CA": "Canada (≥420×540)",
  "idphoto.digital.note.DS160": "USA DS-160 : fichier < 240 Ko, à partir de 600×600px",
  "idphoto.digital.note.US_RENEW": "Renouvellement passeport USA : < 5 Mo",
  "idphoto.digital.note.UK": "Demande UK en ligne : ≥600×750px, < 10 Mo",
  "idphoto.digital.note.ETIAS": "Schengen ETIAS : < 10 Mo",
  "idphoto.digital.note.JP": "Myna Portal Japon : < 600 Ko, 600×730px recommandé",
  "idphoto.digital.note.IN": "Portails Inde : souvent ≤30 Ko requis (parfois ≤50 Ko)",
  "idphoto.digital.note.CA": "Canada : ≥420×540px, sans limite",
  "idphoto.export.format": "Format",
  "idphoto.export.jpg": "JPG",
  "idphoto.export.png": "PNG",
  "idphoto.export.sizeLimit": "Taille max",
  "idphoto.export.kbUnit": "Ko (0 = illimité)",
  "idphoto.export.btn": "Exporter la photo",
  "idphoto.export.done": "Exporté {{info}}",
  "idphoto.tab.photo": "Aperçu photo",
  "idphoto.tab.print": "Planche d'impression",
  "idphoto.paper.label": "Papier",
  "idphoto.paper.4R": "10×15 cm (102×152 mm)",
  "idphoto.paper.A6": "A6 (105×148 mm)",
  "idphoto.paper.A5": "A5 (148×210 mm)",
  "idphoto.paper.A4": "A4 (210×297 mm)",
  "idphoto.orient.portrait": "Portrait",
  "idphoto.orient.landscape": "Paysage",
  "idphoto.print.paperOrient": "Orientation papier",
  "idphoto.print.photoOrient": "Orientation photo",
  "idphoto.print.btnLayout": "Créer la planche",
  "idphoto.print.btnExportPrint": "Exporter la planche",
  "idphoto.print.initial": "Après avoir généré la photo, cliquez « Créer la planche ». Sortie 300 DPI, prête pour imprimeur ou imprimante domestique.",
  "idphoto.print.fitError": "Ce format de papier est trop petit. Choisissez un papier plus grand ou changez l'orientation.",
  "idphoto.print.summary": "{{paper}} | {{fw}}×{{fh}} mm chacune | {{total}} photos ({{cols}} col. × {{rows}} rangées) | {{w}}×{{h}}px @300DPI",
  "idphoto.print.needGenerate": "Générez d'abord la photo.",
  "idphoto.headRatio": "Tête {{pct}}%",
  "idphoto.file.photo": "photo_identite",
  "idphoto.file.print": "planche_impression"
```

- [ ] **Step 4: es.json 新增块**

```json
  "nav.idphoto": "Foto de identificación",
  "freetool.item.idphoto.title": "Herramienta de foto de identificación IA",
  "freetool.item.idphoto.desc": "Recorte de fondo con IA y cambio de color para 24 formatos de foto tipo carné, con hoja de impresión. 100 % local en tu navegador.",
  "idphoto.title": "Herramienta de foto de identificación IA - Redimensionar / Fondo / Impresión | 100mini",
  "idphoto.desc": "Herramienta gratuita de fotos de identificación: recorte de fondo con IA, 24 formatos de pasaporte y visado, control de conformidad del rostro, límite de peso y hoja de impresión. Todo local: tus fotos nunca salen del dispositivo.",
  "idphoto.keywords": "foto de identificación,foto de pasaporte,foto de visado,quitar fondo IA,foto biométrica,redimensionar foto,hoja de impresión,gratis",
  "idphoto.heading": "Herramienta de foto de identificación IA",
  "idphoto.subheading": "Redimensionar · Cambio de fondo IA · Límite de peso · Hoja de impresión — todo local, sin subir fotos",
  "idphoto.step.open": "Abrir una foto",
  "idphoto.step.spec": "Elegir formato",
  "idphoto.step.bg": "Elegir fondo",
  "idphoto.step.ai": "Generar con IA",
  "idphoto.step.compliance": "Control de conformidad y regla facial",
  "idphoto.step.adjust": "Ajuste fino (WYSIWYG)",
  "idphoto.step.export": "Exportar",
  "idphoto.drop.main": "Haz clic o arrastra una foto aquí",
  "idphoto.drop.sub": "JPG / PNG / WEBP · basta una selfie de frente",
  "idphoto.drop.selected": "Seleccionada: {{name}}",
  "idphoto.drop.errType": "Elige un archivo de imagen.",
  "idphoto.drop.errLoad": "No se pudo cargar la imagen. Prueba con otra.",
  "idphoto.region.all": "🌐 Todos los formatos",
  "idphoto.region.cn": "🇨🇳 China",
  "idphoto.region.us": "🇺🇸 EE.UU.",
  "idphoto.region.ca": "🇨🇦 Canadá",
  "idphoto.region.uk": "🇬🇧 Reino Unido",
  "idphoto.region.eu": "🇪🇺 Schengen/UE",
  "idphoto.region.in": "🇮🇳 India",
  "idphoto.region.au": "🇦🇺 Australia",
  "idphoto.region.jp": "🇯🇵 Japón",
  "idphoto.region.kr": "🇰🇷 Corea",
  "idphoto.region.br": "🇧🇷 Brasil",
  "idphoto.region.other": "🔧 Personalizado",
  "idphoto.spec.oneInch.name": "1 pulgada", "idphoto.spec.oneInch.desc": "Currículums y carné estudiantil",
  "idphoto.spec.twoInch.name": "2 pulgadas", "idphoto.spec.twoInch.desc": "Títulos y currículums",
  "idphoto.spec.smallOneInch.name": "1 pulgada pequeña", "idphoto.spec.smallOneInch.desc": "Carné de conducir y reconocimientos",
  "idphoto.spec.smallTwoInch.name": "2 pulgadas pequeña", "idphoto.spec.smallTwoInch.desc": "Pasaportes y visados",
  "idphoto.spec.bigOneInch.name": "1 pulgada grande", "idphoto.spec.bigOneInch.desc": "Algunas inscripciones de exámenes",
  "idphoto.spec.bigTwoInch.name": "2 pulgadas grande", "idphoto.spec.bigTwoInch.desc": "Algunos exámenes oficiales",
  "idphoto.spec.teacherCert.name": "Certif. docente", "idphoto.spec.teacherCert.desc": "Solicitud docente",
  "idphoto.spec.civilServant.name": "Función pública", "idphoto.spec.civilServant.desc": "Oposición nacional",
  "idphoto.spec.accounting.name": "Examen contabilidad", "idphoto.spec.accounting.desc": "Contabilidad junior",
  "idphoto.spec.cet.name": "CET-4/6", "idphoto.spec.cet.desc": "Examen de inglés universitario",
  "idphoto.spec.ncre.name": "NCRE", "idphoto.spec.ncre.desc": "Examen nacional de informática",
  "idphoto.spec.postgrad.name": "Admisión máster", "idphoto.spec.postgrad.desc": "Examen de posgrado",
  "idphoto.spec.socialSecurity.name": "Tarjeta sanitaria", "idphoto.spec.socialSecurity.desc": "Foto seguridad social",
  "idphoto.spec.eDriverLicense.name": "Permiso digital", "idphoto.spec.eDriverLicense.desc": "Carné electrónico",
  "idphoto.spec.fiveInch.name": "5 pulgadas", "idphoto.spec.fiveInch.desc": "Revelado de fotos",
  "idphoto.spec.us2x2.name": "EE.UU. 2×2", "idphoto.spec.us2x2.desc": "Visa y pasaporte EE.UU., 2×2 pulgadas",
  "idphoto.spec.ca50x70.name": "Canadá 50×70", "idphoto.spec.ca50x70.desc": "Visa e inmigración de Canadá",
  "idphoto.spec.uk35x45.name": "R. Unido 35×45", "idphoto.spec.uk35x45.desc": "Foto de visado británico",
  "idphoto.spec.eu35x45.name": "Schengen/UE 35×45", "idphoto.spec.eu35x45.desc": "Visado Schengen y ETIAS",
  "idphoto.spec.in35x45.name": "India 35×45", "idphoto.spec.in35x45.desc": "Foto de visado indio",
  "idphoto.spec.au35x45.name": "Australia 35×45", "idphoto.spec.au35x45.desc": "Foto de visado australiano",
  "idphoto.spec.jp35x45.name": "Japón 35×45", "idphoto.spec.jp35x45.desc": "Foto de visado japonés",
  "idphoto.spec.kr35x45.name": "Corea 35×45", "idphoto.spec.kr35x45.desc": "Foto de visado coreano (solo blanco)",
  "idphoto.spec.br50x70.name": "Brasil 50×70", "idphoto.spec.br50x70.desc": "Foto de visado brasileño",
  "idphoto.spec.custom.name": "Personalizado", "idphoto.spec.custom.desc": "Ancho y alto libres",
  "idphoto.custom.w": "Ancho (px)",
  "idphoto.custom.h": "Alto (px)",
  "idphoto.mmInfo": "{{w}}×{{h}} px · aprox. {{wmm}}×{{hmm}} mm @300DPI",
  "idphoto.bg.white": "Blanco",
  "idphoto.bg.red": "Rojo",
  "idphoto.bg.blue": "Azul",
  "idphoto.bg.navyBlue": "Azul marino",
  "idphoto.bg.lightGray": "Gris claro",
  "idphoto.bg.beige": "Beige",
  "idphoto.bg.lightBlue": "Azul claro",
  "idphoto.bg.keep": "Conservar el fondo original",
  "idphoto.bgNote.us": "⚠️ Las solicitudes digitales de EE.UU. exigen el fondo blanco original (norma del Dept. de Estado) — no uses el cambio de fondo IA.",
  "idphoto.bgNote.in": "⚠️ La mayoría de portales indios exigen fondo blanco puro y orejas visibles; archivos limitados a 20–50 KB.",
  "idphoto.bgNote.kr": "⚠️ Corea solo acepta blanco puro — crema/gris claro se rechaza.",
  "idphoto.btn.ai": "Generar con IA (recorte + encuadre)",
  "idphoto.btn.cropOnly": "Solo recortar (sin IA)",
  "idphoto.ai.hint": "El primer uso descarga el modelo de IA (~88 MB, luego queda en caché). También puedes marcar «Conservar el fondo original» y usar «Solo recortar».",
  "idphoto.status.noPhoto": "Abre primero una foto.",
  "idphoto.status.loaded": "Cargada: {{name}}\nPulsa «Generar con IA» o «Solo recortar».",
  "idphoto.status.faceModel": "Cargando modelo de detección facial…",
  "idphoto.status.runtimeLoading": "Cargando runtime de IA…",
  "idphoto.status.downloading": "Descargando modelo de recorte… {{pct}}%",
  "idphoto.status.compiling": "Modelo descargado, compilando (1–3 min la primera vez)…",
  "idphoto.status.inferring": "Eliminando el fondo con IA (10–30 s)…",
  "idphoto.status.done": "¡Listo! Ajusta si quieres y exporta. Para imprimir, abre la pestaña «Hoja de impresión».",
  "idphoto.status.cropOnlyDone": "Recorte central aplicado. Ajusta si quieres y exporta.",
  "idphoto.status.needAiFirst": "Cambiar el fondo requiere recortar antes: pulsa «Generar con IA». Solo se recorta una vez; luego el cambio de color es instantáneo.",
  "idphoto.ai.failPrefix": "Error de procesamiento IA: ",
  "idphoto.ai.netHint": "Parece un problema de red: el modelo debe descargarse. Las redes corporativas/universitarias pueden bloquearlo; prueba otra red o datos móviles.",
  "idphoto.ai.offlineHint": "También puedes marcar «Conservar el fondo original» y usar «Solo recortar» (sin conexión).",
  "idphoto.compliance.unknown": "La proporción facial se comprueba tras generar",
  "idphoto.compliance.ok": "✓ Proporción conforme: {{pct}}% (requerido {{lo}}–{{hi}}%)",
  "idphoto.compliance.bad": "✗ Proporción fuera de rango: {{pct}}% (requerido {{lo}}–{{hi}}%). Usa el ajuste fino.",
  "idphoto.ruler.show": "Mostrar regla facial en la vista previa",
  "idphoto.tip.1": "Expresión neutra, sin sonreír con los dientes (obligatorio en la mayoría de países)",
  "idphoto.tip.2": "Quítate las gafas: reflejos/monturas prohibidos (EE.UU., R.Unido…)",
  "idphoto.tip.3": "Luz uniforme, sin sombras duras, fondo liso",
  "idphoto.tip.4": "Orejas visibles si es posible (India/algunos visados)",
  "idphoto.tip.5": "Sin maquillage marcado ni cubrecabezas (excepto motivos religiosos)",
  "idphoto.adj.zoom": "Zoom",
  "idphoto.adj.x": "Horizontal",
  "idphoto.adj.y": "Vertical",
  "idphoto.adj.reset": "Restablecer ajustes",
  "idphoto.digital.label": "Preajuste de envío digital",
  "idphoto.digital.none": "Personalizado / ilimitado",
  "idphoto.digital.DS160": "EE.UU. DS-160 (<240 KB)",
  "idphoto.digital.US_RENEW": "Renovación pasaporte EE.UU. (<5 MB)",
  "idphoto.digital.UK": "UK online (≥600×750, <10 MB)",
  "idphoto.digital.ETIAS": "Schengen ETIAS (<10 MB)",
  "idphoto.digital.JP": "Japón Myna (<600 KB)",
  "idphoto.digital.IN": "Portales India (≤30 KB)",
  "idphoto.digital.CA": "Canadá (≥420×540)",
  "idphoto.digital.note.DS160": "EE.UU. DS-160: archivo < 240 KB, desde 600×600px",
  "idphoto.digital.note.US_RENEW": "Renovación pasaporte EE.UU.: < 5 MB",
  "idphoto.digital.note.UK": "Solicitud UK online: ≥600×750px, < 10 MB",
  "idphoto.digital.note.ETIAS": "Schengen ETIAS: < 10 MB",
  "idphoto.digital.note.JP": "Myna Portal Japón: < 600 KB, se recomienda 600×730px",
  "idphoto.digital.note.IN": "Portales India: normalmente ≤30 KB (algunos ≤50 KB)",
  "idphoto.digital.note.CA": "Canadá: ≥420×540px, sin límite",
  "idphoto.export.format": "Formato",
  "idphoto.export.jpg": "JPG",
  "idphoto.export.png": "PNG",
  "idphoto.export.sizeLimit": "Tamaño máx.",
  "idphoto.export.kbUnit": "KB (0 = ilimitado)",
  "idphoto.export.btn": "Exportar foto",
  "idphoto.export.done": "Exportada {{info}}",
  "idphoto.tab.photo": "Vista previa",
  "idphoto.tab.print": "Hoja de impresión",
  "idphoto.paper.label": "Papel",
  "idphoto.paper.4R": "10×15 cm (102×152 mm)",
  "idphoto.paper.A6": "A6 (105×148 mm)",
  "idphoto.paper.A5": "A5 (148×210 mm)",
  "idphoto.paper.A4": "A4 (210×297 mm)",
  "idphoto.orient.portrait": "Vertical",
  "idphoto.orient.landscape": "Horizontal",
  "idphoto.print.paperOrient": "Orientación del papel",
  "idphoto.print.photoOrient": "Orientación de la foto",
  "idphoto.print.btnLayout": "Crear hoja",
  "idphoto.print.btnExportPrint": "Exportar hoja",
  "idphoto.print.initial": "Tras generar la foto, pulsa «Crear hoja». Salida a 300 DPI, lista para tienda de fotos o impresora doméstica.",
  "idphoto.print.fitError": "Este papel no es suficiente. Usa papel más grande o cambia la orientación.",
  "idphoto.print.summary": "{{paper}} | {{fw}}×{{fh}} mm cada una | {{total}} fotos ({{cols}} col. × {{rows}} fil.) | {{w}}×{{h}}px @300DPI",
  "idphoto.print.needGenerate": "Genera primero la foto.",
  "idphoto.headRatio": "Cabeza {{pct}}%",
  "idphoto.file.photo": "foto_identificacion",
  "idphoto.file.print": "hoja_impresion"
```

- [ ] **Step 5: pt.json 新增块**

```json
  "nav.idphoto": "Foto de identificação",
  "freetool.item.idphoto.title": "Ferramenta de foto de identificação com IA",
  "freetool.item.idphoto.desc": "Remoção de fundo com IA e troca de cor para 24 tamanhos de foto 3x4/tipo passe, com folha de impressão. 100 % local no navegador.",
  "idphoto.title": "Ferramenta de foto de identificação IA - Redimensionar / Fundo / Impressão | 100mini",
  "idphoto.desc": "Ferramenta gratuita de foto de identificação: remoção de fundo com IA, 24 tamanhos de passaporte e visto, verificação de conformidade facial, controle de tamanho de arquivo e folha de impressão. Tudo local — suas fotos nunca saem do dispositivo.",
  "idphoto.keywords": "foto de identificação,foto de passaporte,foto de visto,remover fundo IA,foto biométrica,redimensionar foto,folha de impressão,grátis",
  "idphoto.heading": "Ferramenta de foto de identificação IA",
  "idphoto.subheading": "Redimensionar · Troca de fundo IA · Limite de arquivo · Folha de impressão — tudo local, sem upload",
  "idphoto.step.open": "Abrir uma foto",
  "idphoto.step.spec": "Escolher tamanho",
  "idphoto.step.bg": "Escolher fundo",
  "idphoto.step.ai": "Gerar com IA",
  "idphoto.step.compliance": "Conformidade e régua facial",
  "idphoto.step.adjust": "Ajuste fino (WYSIWYG)",
  "idphoto.step.export": "Exportar",
  "idphoto.drop.main": "Clique ou arraste uma foto aqui",
  "idphoto.drop.sub": "JPG / PNG / WEBP · uma selfie de frente é suficiente",
  "idphoto.drop.selected": "Selecionada: {{name}}",
  "idphoto.drop.errType": "Escolha um arquivo de imagem.",
  "idphoto.drop.errLoad": "Falha ao carregar a imagem. Tente outra.",
  "idphoto.region.all": "🌐 Todos os tamanhos",
  "idphoto.region.cn": "🇨🇳 China",
  "idphoto.region.us": "🇺🇸 EUA",
  "idphoto.region.ca": "🇨🇦 Canadá",
  "idphoto.region.uk": "🇬🇧 Reino Unido",
  "idphoto.region.eu": "🇪🇺 Schengen/UE",
  "idphoto.region.in": "🇮🇳 Índia",
  "idphoto.region.au": "🇦🇺 Austrália",
  "idphoto.region.jp": "🇯🇵 Japão",
  "idphoto.region.kr": "🇰🇷 Coreia",
  "idphoto.region.br": "🇧🇷 Brasil",
  "idphoto.region.other": "🔧 Personalizado",
  "idphoto.spec.oneInch.name": "1 polegada", "idphoto.spec.oneInch.desc": "Currículos e carteirinha estudantil",
  "idphoto.spec.twoInch.name": "2 polegadas", "idphoto.spec.twoInch.desc": "Diplomas e currículos",
  "idphoto.spec.smallOneInch.name": "1 polegada pequena", "idphoto.spec.smallOneInch.desc": "CNH e exames médicos",
  "idphoto.spec.smallTwoInch.name": "2 polegadas pequena", "idphoto.spec.smallTwoInch.desc": "Passaportes e vistos",
  "idphoto.spec.bigOneInch.name": "1 polegada grande", "idphoto.spec.bigOneInch.desc": "Algumas inscrições de provas",
  "idphoto.spec.bigTwoInch.name": "2 polegadas grande", "idphoto.spec.bigTwoInch.desc": "Alguns exames profissionais",
  "idphoto.spec.teacherCert.name": "Certif. professor", "idphoto.spec.teacherCert.desc": "Inscrição docente",
  "idphoto.spec.civilServant.name": "Concurso público", "idphoto.spec.civilServant.desc": "Concurso federal",
  "idphoto.spec.accounting.name": "Exame de contabilidade", "idphoto.spec.accounting.desc": "Contabilidade júnior",
  "idphoto.spec.cet.name": "CET-4/6", "idphoto.spec.cet.desc": "Exame de inglês universitário",
  "idphoto.spec.ncre.name": "NCRE", "idphoto.spec.ncre.desc": "Exame nacional de informática",
  "idphoto.spec.postgrad.name": "Admissão mestrado", "idphoto.spec.postgrad.desc": "Exame de pós-graduação",
  "idphoto.spec.socialSecurity.name": "Cartão social", "idphoto.spec.socialSecurity.desc": "Foto previdência social",
  "idphoto.spec.eDriverLicense.name": "CNH digital", "idphoto.spec.eDriverLicense.desc": "Carteira digital",
  "idphoto.spec.fiveInch.name": "5 polegadas", "idphoto.spec.fiveInch.desc": "Impressão de fotos",
  "idphoto.spec.us2x2.name": "EUA 2×2", "idphoto.spec.us2x2.desc": "Visto e passaporte EUA, 2×2 pol.",
  "idphoto.spec.ca50x70.name": "Canadá 50×70", "idphoto.spec.ca50x70.desc": "Visto e imigração do Canadá",
  "idphoto.spec.uk35x45.name": "R. Unido 35×45", "idphoto.spec.uk35x45.desc": "Foto de visto britânico",
  "idphoto.spec.eu35x45.name": "Schengen/UE 35×45", "idphoto.spec.eu35x45.desc": "Visto Schengen e ETIAS",
  "idphoto.spec.in35x45.name": "Índia 35×45", "idphoto.spec.in35x45.desc": "Foto de visto indiano",
  "idphoto.spec.au35x45.name": "Austrália 35×45", "idphoto.spec.au35x45.desc": "Foto de visto australiano",
  "idphoto.spec.jp35x45.name": "Japão 35×45", "idphoto.spec.jp35x45.desc": "Foto de visto japonês",
  "idphoto.spec.kr35x45.name": "Coreia 35×45", "idphoto.spec.kr35x45.desc": "Foto de vista coreano (somente branco)",
  "idphoto.spec.br50x70.name": "Brasil 50×70", "idphoto.spec.br50x70.desc": "Foto de visto brasileiro",
  "idphoto.spec.custom.name": "Personalizado", "idphoto.spec.custom.desc": "Largura e altura livres",
  "idphoto.custom.w": "Largura (px)",
  "idphoto.custom.h": "Altura (px)",
  "idphoto.mmInfo": "{{w}}×{{h}} px · aprox. {{wmm}}×{{hmm}} mm @300DPI",
  "idphoto.bg.white": "Branco",
  "idphoto.bg.red": "Vermelho",
  "idphoto.bg.blue": "Azul",
  "idphoto.bg.navyBlue": "Azul-marinho",
  "idphoto.bg.lightGray": "Cinza claro",
  "idphoto.bg.beige": "Bege",
  "idphoto.bg.lightBlue": "Azul claro",
  "idphoto.bg.keep": "Manter o fundo original",
  "idphoto.bgNote.us": "⚠️ Pedidos digitais dos EUA exigem o fundo branco original (norma do Dept. de Estado) — não use a troca de fundo por IA.",
  "idphoto.bgNote.in": "⚠️ A maioria dos portais indianos exige fundo branco puro e orelhas visíveis; arquivos geralmente limitados a 20–50 KB.",
  "idphoto.bgNote.kr": "⚠️ A Coreia só aceita branco puro — creme/cinza claro é rejeitado.",
  "idphoto.btn.ai": "Gerar com IA (recorte + enquadramento)",
  "idphoto.btn.cropOnly": "Somente recortar (sem IA)",
  "idphoto.ai.hint": "O primeiro uso baixa o modelo de IA (~88 MB, depois fica em cache). Você também pode marcar «Manter o fundo original» e usar «Somente recortar».",
  "idphoto.status.noPhoto": "Abra primeiro uma foto.",
  "idphoto.status.loaded": "Carregada: {{name}}\nClique em «Gerar com IA» ou «Somente recortar».",
  "idphoto.status.faceModel": "Carregando modelo de detecção facial…",
  "idphoto.status.runtimeLoading": "Carregando runtime de IA…",
  "idphoto.status.downloading": "Baixando modelo de recorte… {{pct}}%",
  "idphoto.status.compiling": "Modelo baixado, compilando (1–3 min na primeira vez)…",
  "idphoto.status.inferring": "Removendo o fundo com IA (10–30 s)…",
  "idphoto.status.done": "Pronto! Ajuste se quiser e exporte. Para imprimir, abra a aba «Folha de impressão».",
  "idphoto.status.cropOnlyDone": "Recorte central aplicado. Ajuste se quiser e exporte.",
  "idphoto.status.needAiFirst": "Trocar o fundo exige recortar antes: clique em «Gerar com IA». O recorte é único; depois as trocas são instantâneas.",
  "idphoto.ai.failPrefix": "Falha no processamento da IA: ",
  "idphoto.ai.netHint": "Parece problema de rede: o modelo precisa ser baixado. Redes corporativas/universitárias podem bloquear; tente outra rede ou dados móveis.",
  "idphoto.ai.offlineHint": "Ou marque «Manter o fundo original» e use «Somente recortar» (sem internet).",
  "idphoto.compliance.unknown": "A proporção facial será verificada após gerar",
  "idphoto.compliance.ok": "✓ Proporção adequada: {{pct}}% (exigido {{lo}}–{{hi}}%)",
  "idphoto.compliance.bad": "✗ Proporção fora da faixa: {{pct}}% (exigido {{lo}}–{{hi}}%). Use o ajuste fino.",
  "idphoto.ruler.show": "Mostrar régua facial na prévia",
  "idphoto.tip.1": "Expressão neutra, sem sorrir mostrando os dentes (obrigatório na maioria dos países)",
  "idphoto.tip.2": "Sem óculos — reflexos/armações proibidos (EUA, Reino Unido…)",
  "idphoto.tip.3": "Luz uniforme, sem sombras fortes, fundo liso",
  "idphoto.tip.4": "Orelhas visíveis quando possível (Índia/alguns vistos)",
  "idphoto.tip.5": "Sem maquiagem pesada nem cobertura de cabeça (exceto motivo religioso)",
  "idphoto.adj.zoom": "Zoom",
  "idphoto.adj.x": "Horizontal",
  "idphoto.adj.y": "Vertical",
  "idphoto.adj.reset": "Redefinir ajustes",
  "idphoto.digital.label": "Predefinição de envio digital",
  "idphoto.digital.none": "Personalizado / ilimitado",
  "idphoto.digital.DS160": "EUA DS-160 (<240 KB)",
  "idphoto.digital.US_RENEW": "Renovação de passaporte EUA (<5 MB)",
  "idphoto.digital.UK": "UK online (≥600×750, <10 MB)",
  "idphoto.digital.ETIAS": "Schengen ETIAS (<10 MB)",
  "idphoto.digital.JP": "Japão Myna (<600 KB)",
  "idphoto.digital.IN": "Portais da Índia (≤30 KB)",
  "idphoto.digital.CA": "Canadá (≥420×540)",
  "idphoto.digital.note.DS160": "EUA DS-160: arquivo < 240 KB, a partir de 600×600px",
  "idphoto.digital.note.US_RENEW": "Renovação de passaporte EUA: < 5 MB",
  "idphoto.digital.note.UK": "Pedido UK online: ≥600×750px, < 10 MB",
  "idphoto.digital.note.ETIAS": "Schengen ETIAS: < 10 MB",
  "idphoto.digital.note.JP": "Myna Portal Japão: < 600 KB, recomenda-se 600×730px",
  "idphoto.digital.note.IN": "Portais da Índia: geralmente ≤30 KB (alguns ≤50 KB)",
  "idphoto.digital.note.CA": "Canadá: ≥420×540px, sem limite",
  "idphoto.export.format": "Formato",
  "idphoto.export.jpg": "JPG",
  "idphoto.export.png": "PNG",
  "idphoto.export.sizeLimit": "Limite de tamanho",
  "idphoto.export.kbUnit": "KB (0 = ilimitado)",
  "idphoto.export.btn": "Exportar foto",
  "idphoto.export.done": "Exportada {{info}}",
  "idphoto.tab.photo": "Prévia da foto",
  "idphoto.tab.print": "Folha de impressão",
  "idphoto.paper.label": "Papel",
  "idphoto.paper.4R": "10×15 cm (102×152 mm)",
  "idphoto.paper.A6": "A6 (105×148 mm)",
  "idphoto.paper.A5": "A5 (148×210 mm)",
  "idphoto.paper.A4": "A4 (210×297 mm)",
  "idphoto.orient.portrait": "Retrato",
  "idphoto.orient.landscape": "Paisagem",
  "idphoto.print.paperOrient": "Orientação do papel",
  "idphoto.print.photoOrient": "Orientação da foto",
  "idphoto.print.btnLayout": "Criar folha",
  "idphoto.print.btnExportPrint": "Exportar folha",
  "idphoto.print.initial": "Após gerar a foto, clique em «Criar folha». Saída em 300 DPI, pronta para gráfica ou impressora doméstica.",
  "idphoto.print.fitError": "Este papel não comporta a foto. Use papel maior ou mude a orientação.",
  "idphoto.print.summary": "{{paper}} | {{fw}}×{{fh}} mm cada | {{total}} fotos ({{cols}} col. × {{rows}} lin.) | {{w}}×{{h}}px @300DPI",
  "idphoto.print.needGenerate": "Gere primeiro a foto.",
  "idphoto.headRatio": "Cabeça {{pct}}%",
  "idphoto.file.photo": "foto_identificacao",
  "idphoto.file.print": "folha_impressao"
```

注：`idphoto.paper.4R` 在 en 里含双引号 `4×6"` —— JSON 中写成 `'4×6\" (102×152mm)'` 需转义为 `"4×6\" (102×152mm)"`；其余语言用 cm 措辞避免引号。

- [ ] **Step 6: 校验 JSON 合法 + key 一致性**

Run:
```bash
node -e "
const fs=require('fs');
const langs=['zh','en','fr','es','pt'];
const keys=langs.map(l=>Object.keys(JSON.parse(fs.readFileSync('app/lib/locales/'+l+'.json','utf8'))).filter(k=>k.startsWith('idphoto.')||k==='nav.idphoto'||k.startsWith('freetool.item.idphoto')).sort());
const base=JSON.stringify(keys[0]);
langs.forEach((l,i)=>{ if(JSON.stringify(keys[i])!==base){console.error('MISMATCH',l);process.exit(1);} });
console.log('OK',keys[0].length,'keys');
"
```
Expected: `OK 138 keys`（数量以实际为准，关键是 MISMATCH 不出现）

- [ ] **Step 7: Commit**

```bash
git add app/lib/locales/*.json
git commit -m "i18n(idphoto): 新增证件照工具五语言文案"
```

---

### Task 5: 路由骨架 + 上传/裁剪/预览垂直闭环（含 Playwright 冒烟测试）

**Files:**
- Create: `app/routes/idphoto.tsx`
- Create: `app/components/idphoto/PhotoDropzone.tsx`
- Create: `app/components/idphoto/PreviewCanvas.tsx`
- Create: `app/components/idphoto/IdPhotoWorkbench.tsx`
- Test: `tests/idphoto.spec.ts`

- [ ] **Step 1: 先写失败的冒烟测试**

`tests/idphoto.spec.ts`：

```ts
import { test, expect } from "@playwright/test";

test("idphoto 工作台渲染", async ({ page }) => {
  await page.goto("/idphoto");
  await expect(page.getByTestId("idphoto-heading")).toBeVisible();
  await expect(page.getByTestId("idphoto-dropzone")).toBeVisible();
  await expect(page.getByTestId("idphoto-status")).toContainText(
    /打开照片|Open a Photo|Ouvrir|Abre|Abra/i,
  );
});
```

- [ ] **Step 2: 运行确认失败**

Run: `npx playwright test tests/idphoto.spec.ts`
Expected: FAIL（404，路由不存在）

- [ ] **Step 3: 创建 PhotoDropzone.tsx**

```tsx
import { useRef, useState } from "react";
import { useTranslation } from "react-i18next";
import { Upload } from "lucide-react";
import { cn } from "~/lib/utils";

interface PhotoDropzoneProps {
  /** 已选文件的描述行；空串则显示默认提示 */
  selectedLabel?: string;
  onFile: (file: File) => void;
}

export function PhotoDropzone({ selectedLabel, onFile }: PhotoDropzoneProps) {
  const { t } = useTranslation();
  const [dragOver, setDragOver] = useState(false);
  const inputRef = useRef<HTMLInputElement>(null);

  return (
    <div>
      <div
        data-testid="idphoto-dropzone"
        role="button"
        tabIndex={0}
        onClick={() => inputRef.current?.click()}
        onKeyDown={(e) => e.key === "Enter" && inputRef.current?.click()}
        onDragOver={(e) => {
          e.preventDefault();
          setDragOver(true);
        }}
        onDragLeave={() => setDragOver(false)}
        onDrop={(e) => {
          e.preventDefault();
          setDragOver(false);
          const f = e.dataTransfer.files?.[0];
          if (f) onFile(f);
        }}
        className={cn(
          "flex cursor-pointer flex-col items-center justify-center rounded-xl border-2 border-dashed p-6 text-center transition-all",
          dragOver
            ? "border-primary bg-primary/5"
            : "border-muted-foreground/30 bg-muted/30 hover:border-muted-foreground/50",
        )}
      >
        <Upload className="mb-1.5 size-6 text-muted-foreground" />
        <div className="text-sm font-bold text-foreground">{t("idphoto.drop.main")}</div>
        <div className="mt-1 text-[11px] leading-relaxed text-muted-foreground">
          {selectedLabel || t("idphoto.drop.sub")}
        </div>
      </div>
      <input
        ref={inputRef}
        type="file"
        accept="image/*"
        className="hidden"
        onChange={(e) => {
          const f = e.target.files?.[0];
          if (f) onFile(f);
          e.target.value = "";
        }}
      />
    </div>
  );
}
```

- [ ] **Step 4: 创建 PreviewCanvas.tsx**

```tsx
import type { RefObject } from "react";

interface PreviewCanvasProps {
  resultRef: RefObject<HTMLCanvasElement | null>;
  rulerRef: RefObject<HTMLCanvasElement | null>;
  W: number;
  H: number;
}

/** 结果画布 + 头身比标尺 overlay 双层 canvas */
export function PreviewCanvas({ resultRef, rulerRef, W, H }: PreviewCanvasProps) {
  return (
    <div
      className="relative mx-auto inline-block max-w-full overflow-hidden rounded-xl border border-border"
      style={{
        backgroundImage: "repeating-conic-gradient(#d1d5db 0% 25%, #ffffff 0% 50%)",
        backgroundSize: "20px 20px",
      }}
    >
      <canvas ref={resultRef} width={W} height={H} className="block h-auto max-w-full" />
      <canvas ref={rulerRef} width={W} height={H} className="pointer-events-none absolute inset-0 h-full w-full" />
    </div>
  );
}
```

- [ ] **Step 5: 创建 IdPhotoWorkbench.tsx（Task 5 版本，预留插入锚点注释）**

```tsx
import { useCallback, useEffect, useMemo, useRef, useState } from "react";
import type { ReactNode } from "react";
import { useTranslation } from "react-i18next";
import { Button } from "~/components/ui/button";
import { PhotoDropzone } from "./PhotoDropzone";
import { PreviewCanvas } from "./PreviewCanvas";
import { SIZE_PRESETS, currentSize, headRange, headTarget } from "~/lib/idphoto/specs";
import { complianceRatio, computeBase, drawRuler, renderCompose } from "~/lib/idphoto/compose";
import { detectFace } from "~/lib/idphoto/face";
import { segmentImage, type SegEvent } from "~/lib/idphoto/segmentation";

const ADJUST_DEFAULT = { zoom: 100, x: 0, y: 0 };

export function StepTitle({ n, children }: { n: number; children: ReactNode }) {
  return (
    <h2 className="mb-3 flex items-center gap-2 text-sm font-semibold text-foreground">
      <span className="inline-flex size-[22px] shrink-0 items-center justify-center rounded-full bg-primary text-xs font-bold text-primary-foreground">
        {n}
      </span>
      {children}
    </h2>
  );
}

export function IdPhotoWorkbench() {
  const { t } = useTranslation();
  const resultRef = useRef<HTMLCanvasElement>(null);
  const rulerRef = useRef<HTMLCanvasElement>(null);

  // ===== 状态（与设计文档第 3 节一致）=====
  const [srcImg, setSrcImg] = useState<HTMLImageElement | null>(null);
  const [cutImg, setCutImg] = useState<HTMLImageElement | null>(null);
  const [faceBox, setFaceBox] = useState<Parameters<typeof computeBase>[0]["faceBox"]>(null);
  const [personTop, setPersonTop] = useState<number | null>(null);
  const [fileLabel, setFileLabel] = useState("");
  const [presetIdx, setPresetIdx] = useState(0);
  const [customW, setCustomW] = useState(SIZE_PRESETS[0].w);
  const [customH, setCustomH] = useState(SIZE_PRESETS[0].h);
  const [bgColor, setBgColor] = useState("#FFFFFF");
  const [keepBg, setKeepBg] = useState(false);
  const [digitalKey, setDigitalKey] = useState("");
  const [sizeLimitKB, setSizeLimitKB] = useState(0);
  const [adjust, setAdjust] = useState(ADJUST_DEFAULT);
  const [resultReady, setResultReady] = useState(false);
  const [aiBusy, setAiBusy] = useState(false);
  const [showRuler, setShowRuler] = useState(true);
  const [segEvent, setSegEvent] = useState<SegEvent | null>(null);
  const [status, setStatus] = useState<{ kind: "ok" | "err"; text: string }>({ kind: "ok", text: "" });

  const setStatusOk = useCallback((text: string) => setStatus({ kind: "ok", text }), []);
  const setStatusErr = useCallback((text: string) => setStatus({ kind: "err", text }), []);

  // ===== 派生值 =====
  const preset = SIZE_PRESETS[presetIdx] ?? SIZE_PRESETS[0];
  const effectiveSize = useMemo(() => currentSize(presetIdx, customW, customH), [presetIdx, customW, customH]);
  const [loRatio, hiRatio] = useMemo(() => headRange(preset), [preset]);
  const target = useMemo(() => headTarget(preset), [preset]);

  const { base, headSrc } = useMemo(() => {
    if (!srcImg || !resultReady) return { base: null, headSrc: null };
    return computeBase({
      srcW: srcImg.naturalWidth,
      srcH: srcImg.naturalHeight,
      W: effectiveSize.w,
      H: effectiveSize.h,
      faceBox,
      personTop,
      target,
    });
  }, [srcImg, resultReady, effectiveSize.w, effectiveSize.h, faceBox, personTop, target]);

  const headRatioPct = useMemo(() => {
    if (!headSrc || !base) return null;
    return Math.round(complianceRatio({ headSrc, base, zoom: adjust.zoom, H: effectiveSize.h }) * 100);
  }, [headSrc, base, adjust.zoom, effectiveSize.h]);

  // ===== 绘制（结果层 + 标尺层）=====
  useEffect(() => {
    const canvas = resultRef.current;
    const ruler = rulerRef.current;
    if (!canvas || !ruler) return;
    const s = effectiveSize;
    canvas.width = s.w;
    canvas.height = s.h;
    ruler.width = s.w;
    ruler.height = s.h;
    const ctx = canvas.getContext("2d");
    const rctx = ruler.getContext("2d");
    if (!ctx || !rctx) return;

    ctx.fillStyle = keepBg ? "#FFFFFF" : bgColor;
    ctx.fillRect(0, 0, s.w, s.h);
    rctx.clearRect(0, 0, s.w, s.h);

    if (srcImg && base && resultReady) {
      renderCompose(ctx, {
        img: cutImg ?? srcImg,
        srcW: srcImg.naturalWidth,
        srcH: srcImg.naturalHeight,
        W: s.w,
        H: s.h,
        base,
        zoom: adjust.zoom,
        adjX: adjust.x,
        adjY: adjust.y,
      });
      if (showRuler && headSrc && headRatioPct != null) {
        drawRuler(rctx, {
          headSrc,
          base,
          srcW: srcImg.naturalWidth,
          srcH: srcImg.naturalHeight,
          zoom: adjust.zoom,
          adjX: adjust.x,
          adjY: adjust.y,
          W: s.w,
          H: s.h,
          lo: loRatio,
          hi: hiRatio,
          label: t("idphoto.headRatio", { pct: headRatioPct }),
        });
      }
    }
  }, [
    effectiveSize, base, headSrc, srcImg, cutImg, bgColor, keepBg, adjust,
    resultReady, showRuler, loRatio, hiRatio, headRatioPct, t,
  ]);

  // ===== 行为 =====
  const loadPhotoFile = useCallback(
    (file: File) => {
      if (!file.type.startsWith("image/")) {
        setStatusErr(t("idphoto.drop.errType"));
        return;
      }
      const url = URL.createObjectURL(file);
      const img = new Image();
      img.onload = () => {
        URL.revokeObjectURL(url);
        setSrcImg(img);
        setCutImg(null);
        setFaceBox(null);
        setPersonTop(null);
        setResultReady(false);
        setAdjust(ADJUST_DEFAULT);
        setFileLabel(`${file.name}（${img.naturalWidth}×${img.naturalHeight}px）`);
        setStatusOk(t("idphoto.status.loaded", { name: file.name }));
      };
      img.onerror = () => {
        URL.revokeObjectURL(url);
        setStatusErr(t("idphoto.drop.errLoad"));
      };
      img.src = url;
    },
    [t, setStatusOk, setStatusErr],
  );

  const resetAdjust = useCallback(() => setAdjust(ADJUST_DEFAULT), []);

  const runCropOnly = useCallback(() => {
    if (!srcImg) {
      setStatusErr(t("idphoto.status.noPhoto"));
      return;
    }
    setCutImg(null);
    setFaceBox(null);
    setPersonTop(null);
    resetAdjust();
    setResultReady(true);
    setStatusOk(t("idphoto.status.cropOnlyDone"));
  }, [srcImg, t, resetAdjust, setStatusOk, setStatusErr]);

  // TASK7: runAI 在此追加

  // segEvent → 状态文案（TASK7 启用）
  useEffect(() => {
    if (!segEvent) return;
    switch (segEvent.kind) {
      case "runtimeLoading":
        setStatusOk(t("idphoto.status.runtimeLoading"));
        break;
      case "modelDownloading":
        setStatusOk(t("idphoto.status.downloading", { pct: segEvent.pct }));
        break;
      case "modelCompiling":
        setStatusOk(t("idphoto.status.compiling"));
        break;
      case "inferring":
        setStatusOk(t("idphoto.status.inferring"));
        break;
    }
  }, [segEvent, t, setStatusOk]);

  return (
    <div className="mx-auto grid max-w-7xl gap-6 px-4 py-8 lg:grid-cols-[380px_minmax(0,1fr)]">
      {/* 左：控制面板 */}
      <div className="space-y-4 lg:max-h-[calc(100vh-120px)] lg:overflow-y-auto lg:pr-1">
        <section className="rounded-2xl border border-border bg-card p-4">
          <StepTitle n={1}>{t("idphoto.step.open")}</StepTitle>
          <PhotoDropzone selectedLabel={fileLabel} onFile={loadPhotoFile} />
        </section>

        {/* TASK6: 规格选择 SpecPicker（含自定义宽高） */}
        {/* TASK6: 底色选择 BgColorPicker */}

        <section className="rounded-2xl border border-border bg-card p-4">
          <StepTitle n={4}>{t("idphoto.step.ai")}</StepTitle>
          <Button
            className="w-full"
            disabled={!srcImg || aiBusy}
            onClick={runCropOnly}
            variant="secondary"
          >
            {t("idphoto.btn.cropOnly")}
          </Button>
          {/* TASK7: 「一键 AI 生成」按钮 + ai.hint 提示替换本节内容 */}
        </section>

        {/* TASK7: 合规检测 CompliancePanel */}
        {/* TASK7: 微调 AdjustPanel */}
        {/* TASK8: 导出 ExportPanel */}

        <div
          data-testid="idphoto-status"
          className={
            "whitespace-pre-wrap rounded-lg px-3 py-2.5 text-xs leading-relaxed " +
            (status.kind === "err"
              ? "bg-destructive/10 text-destructive"
              : "bg-muted text-muted-foreground")
          }
        >
          {status.text || t("idphoto.status.noPhoto")}
        </div>
      </div>

      {/* 右：预览区 */}
      <div className="min-w-0">
        {/* TASK9: Tabs 包裹（photo/print），print 内容为 PrintLayoutPanel */}
        <PreviewCanvas resultRef={resultRef} rulerRef={rulerRef} W={effectiveSize.w} H={effectiveSize.h} />
        <p className="mt-3 text-center text-xs text-muted-foreground">{t("idphoto.ruler.show")}</p>
      </div>
    </div>
  );
}
```

注：
- `showRuler` 的开关 UI 在 Task 7 的 CompliancePanel 中提供；Task 5 阶段固定开启。
- `digitalKey/sizeLimitKB` 状态先建好，Task 8 的 ExportPanel 直接消费。

- [ ] **Step 6: 创建路由 app/routes/idphoto.tsx**

```tsx
import { createFileRoute } from "@tanstack/react-router";
import { AppNav } from "~/components/HomeHeader";
import { AppFooter } from "~/components/AppFooter";
import { IdPhotoWorkbench } from "~/components/idphoto/IdPhotoWorkbench";
import i18n from "~/lib/i18n";

export const Route = createFileRoute("/idphoto")({
  head: () => ({
    meta: [
      { title: i18n.t("idphoto.title") },
      { name: "description", content: i18n.t("idphoto.desc") },
      { name: "keywords", content: i18n.t("idphoto.keywords") },
      { name: "robots", content: "index, follow" },
      { property: "og:type", content: "website" },
      { property: "og:title", content: i18n.t("idphoto.heading") },
      { property: "og:description", content: i18n.t("idphoto.desc") },
      { name: "twitter:card", content: "summary" },
      { name: "twitter:title", content: i18n.t("idphoto.heading") },
      { name: "twitter:description", content: i18n.t("idphoto.desc") },
    ],
  }),
  component: IdPhotoPage,
});

function IdPhotoPage() {
  return (
    <div className="flex min-h-screen flex-col">
      <AppNav />
      <main className="flex-1">
        <section className="border-b border-border/60 bg-[#006c49] py-10 dark:bg-[#0b1c30]">
          <div className="mx-auto max-w-7xl px-4 text-center">
            <h1
              data-testid="idphoto-heading"
              className="text-3xl font-extrabold tracking-tight text-white sm:text-4xl"
            >
              {i18n.t("idphoto.heading")}
            </h1>
            <p className="mx-auto mt-3 max-w-2xl text-sm leading-relaxed text-white/80 sm:text-base">
              {i18n.t("idphoto.subheading")}
            </p>
          </div>
        </section>
        <IdPhotoWorkbench />
      </main>
      <AppFooter />
    </div>
  );
}
```

- [ ] **Step 7: 运行测试验证通过 + 类型检查**

Run: `npx playwright test tests/idphoto.spec.ts`
Expected: PASS

Run: `npx tsc --noEmit`
Expected: 无新错误

- [ ] **Step 8: 手测裁剪闭环**

Run: `npm run dev` 后浏览器打开 `http://localhost:5173/idphoto`
Expected: 拖入任一图片 → 状态行显示「已加载…」→ 点「仅裁剪排版」→ 右侧出现居中裁剪预览（一寸比例白底）

- [ ] **Step 9: Commit**

```bash
git add app/routes/idphoto.tsx app/components/idphoto tests/idphoto.spec.ts
git commit -m "feat(idphoto): 路由骨架与上传裁剪预览闭环"
```

---

### Task 6: 规格选择 + 底色选择

**Files:**
- Create: `app/components/idphoto/SpecPicker.tsx`
- Create: `app/components/idphoto/BgColorPicker.tsx`
- Modify: `app/components/idphoto/IdPhotoWorkbench.tsx`

- [ ] **Step 1: 创建 SpecPicker.tsx**

```tsx
import { useTranslation } from "react-i18next";
import { REGIONS, SIZE_PRESETS } from "~/lib/idphoto/specs";

interface SpecPickerProps {
  presetIdx: number;
  regionFilter: string;
  customW: number;
  customH: number;
  onRegion: (v: string) => void;
  onSelect: (idx: number) => void;
  onCustomSize: (w: number, h: number) => void;
}

export function SpecPicker({ presetIdx, regionFilter, customW, customH, onRegion, onSelect, onCustomSize }: SpecPickerProps) {
  const { t } = useTranslation();
  const visible = SIZE_PRESETS.map((p, i) => ({ p, i })).filter(
    ({ p }) => regionFilter === "all" || p.group === regionFilter,
  );
  const isCustom = SIZE_PRESETS[presetIdx]?.key === "custom";

  return (
    <div>
      <select
        aria-label={t("idphoto.step.spec")}
        value={regionFilter}
        onChange={(e) => onRegion(e.target.value)}
        className="mb-2.5 w-full rounded-lg border border-input bg-background px-2.5 py-2 text-sm"
      >
        {REGIONS.map((r) => (
          <option key={r.value} value={r.value}>
            {t(`idphoto.region.${r.value}`)}
          </option>
        ))}
      </select>
      <div className="grid max-h-64 grid-cols-2 gap-2 overflow-y-auto">
        {visible.map(({ p, i }) => {
          const active = i === presetIdx;
          return (
            <button
              key={p.key}
              type="button"
              onClick={() => onSelect(i)}
              className={
                "rounded-xl border p-2 text-left transition-all " +
                (active
                  ? "border-primary bg-primary/10 shadow-[0_0_0_2px] shadow-primary/15"
                  : "border-border hover:border-primary/40")
              }
            >
              <div className="flex items-center gap-1 text-[13px] font-bold text-foreground">
                <span>{t(`idphoto.spec.${p.key}.name`)}</span>
                <span className="ml-auto text-xs">{p.flag}</span>
              </div>
              <div className="mt-1 text-[10px] text-muted-foreground">
                {p.key === "custom" ? t("idphoto.spec.custom.desc") : `${p.w}×${p.h}px`}
              </div>
              <div className="mt-0.5 text-[10px] text-muted-foreground/80">{t(`idphoto.spec.${p.key}.desc`)}</div>
            </button>
          );
        })}
      </div>

      {isCustom && (
        <div className="mt-3 flex items-center gap-2">
          <input
            type="number"
            min={1}
            aria-label={t("idphoto.custom.w")}
            value={customW}
            onChange={(e) => onCustomSize(Math.max(1, +e.target.value || 1), customH)}
            className="w-full rounded-lg border border-input bg-background px-2.5 py-2 text-sm"
          />
          <input
            type="number"
            min={1}
            aria-label={t("idphoto.custom.h")}
            value={customH}
            onChange={(e) => onCustomSize(customW, Math.max(1, +e.target.value || 1))}
            className="w-full rounded-lg border border-input bg-background px-2.5 py-2 text-sm"
          />
        </div>
      )}

      <div className="mt-2 rounded-lg bg-muted px-3 py-2 text-xs text-muted-foreground">
        {t("idphoto.mmInfo", {
          w: SIZE_PRESETS[presetIdx]?.key === "custom" ? customW : SIZE_PRESETS[presetIdx].w,
          h: SIZE_PRESETS[presetIdx]?.key === "custom" ? customH : SIZE_PRESETS[presetIdx].h,
          wmm: currentWmm(presetIdx, customW, customH).toFixed(1),
          hmm: currentHmm(presetIdx, customW, customH).toFixed(1),
        })}
      </div>
    </div>
  );
}

import { currentSize } from "~/lib/idphoto/specs";
function currentWmm(idx: number, w: number, h: number) {
  return currentSize(idx, w, h).wmm;
}
function currentHmm(idx: number, w: number, h: number) {
  return currentSize(idx, w, h).hmm;
}
```

（实现时把 `import { currentSize }` 合并到顶部第一条 import，这里分开展示仅为醒目。）

- [ ] **Step 2: 创建 BgColorPicker.tsx**

```tsx
import { useTranslation } from "react-i18next";
import { BG_COLORS, type SizePreset } from "~/lib/idphoto/specs";
import { cn } from "~/lib/utils";

interface BgColorPickerProps {
  preset: SizePreset;
  bgColor: string;
  keepBg: boolean;
  onColor: (hex: string) => void;
  onKeepBg: (v: boolean) => void;
}

export function BgColorPicker({ preset, bgColor, keepBg, onColor, onKeepBg }: BgColorPickerProps) {
  const { t } = useTranslation();
  const list = preset.bgOptions ?? BG_COLORS.map((c) => c.value);

  return (
    <div>
      <div className="mb-3 flex flex-wrap gap-2.5">
        {list.map((hex) => {
          const nameKey = BG_COLORS.find((c) => c.value.toUpperCase() === hex.toUpperCase())?.key;
          return (
            <button
              key={hex}
              type="button"
              title={nameKey ? t(`idphoto.bg.${nameKey}`) : hex}
              onClick={() => onColor(hex)}
              className={cn(
                "relative size-9 rounded-lg shadow-[inset_0_0_0_1px_rgba(0,0,0,.08)] transition-all",
                bgColor.toUpperCase() === hex.toUpperCase()
                  ? "ring-2 ring-primary ring-offset-2 ring-offset-card"
                  : "",
              )}
              style={{ background: hex }}
            >
              <span className="absolute -bottom-4 left-1/2 -translate-x-1/2 whitespace-nowrap text-[10px] text-muted-foreground">
                {nameKey ? t(`idphoto.bg.${nameKey}`) : ""}
              </span>
            </button>
          );
        })}
      </div>
      <div className="mb-1 h-4" />
      <div className="flex items-center gap-2">
        <input
          type="color"
          value={bgColor}
          onChange={(e) => onColor(e.target.value)}
          aria-label={t("idphoto.step.bg")}
          className="h-8 w-11 cursor-pointer rounded-lg border border-input bg-background p-0.5"
        />
        <input
          type="text"
          value={bgColor.toUpperCase()}
          maxLength={7}
          spellCheck={false}
          onChange={(e) => {
            let v = e.target.value.trim();
            if (!v.startsWith("#")) v = "#" + v;
            if (/^#[0-9a-fA-F]{6}$/.test(v)) onColor(v);
          }}
          className="w-24 rounded-lg border border-input bg-background px-2 py-1.5 font-mono text-xs uppercase"
        />
        <label className="ml-auto flex cursor-pointer items-center gap-1.5 whitespace-nowrap text-xs text-foreground">
          <input
            type="checkbox"
            checked={keepBg}
            onChange={(e) => onKeepBg(e.target.checked)}
            className="accent-[#006c49]"
          />
          {t("idphoto.bg.keep")}
        </label>
      </div>
      {preset.bgNoteKey && (
        <div className="mt-2 rounded-lg bg-amber-500/10 px-3 py-2 text-xs leading-relaxed text-amber-600 dark:text-amber-400">
          {t(`idphoto.bgNote.${preset.bgNoteKey}`)}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 3: 接入 Workbench**

Modify `IdPhotoWorkbench.tsx`：

1. import 增加：

```tsx
import { SpecPicker } from "./SpecPicker";
import { BgColorPicker } from "./BgColorPicker";
```

2. 在「打开照片」section 之后、`{/* TASK6: 规格选择 SpecPicker */}` 锚点处替换为：

```tsx
        <section className="rounded-2xl border border-border bg-card p-4">
          <StepTitle n={2}>{t("idphoto.step.spec")}</StepTitle>
          <SpecPicker
            presetIdx={presetIdx}
            regionFilter={regionFilter}
            customW={customW}
            customH={customH}
            onRegion={(v) => setRegionFilter(v)}
            onSelect={selectSpec}
            onCustomSize={(w, h) => {
              setCustomW(w);
              setCustomH(h);
            }}
          />
        </section>
```

同时把 `{/* TASK6: 底色选择 BgColorPicker */}` 替换为：

```tsx
        <section className="rounded-2xl border border-border bg-card p-4">
          <StepTitle n={3}>{t("idphoto.step.bg")}</StepTitle>
          <BgColorPicker
            preset={preset}
            bgColor={bgColor}
            keepBg={keepBg}
            onColor={handleBgColor}
            onKeepBg={(v) => setKeepBg(v)}
          />
        </section>
```

3. 状态区补一个新 state（放在 `digitalKey` 之后）：

```tsx
const [regionFilter, setRegionFilter] = useState("all");
```

4. 行为区新增两个回调（放 `runCropOnly` 之后）：

```tsx
  /** 选中规格：重置底色/保留背景，并按规格同步数字提交预设 */
  const selectSpec = useCallback(
    (i: number) => {
      const p = SIZE_PRESETS[i];
      setPresetIdx(i);
      setKeepBg(false);
      if (p.key !== "custom") {
        setCustomW(p.w);
        setCustomH(p.h);
      }
      if (p.digitalKey) {
        setDigitalKey(p.digitalKey);
        setSizeLimitKB(DIGITAL[p.digitalKey].maxKB);
      } else {
        setDigitalKey("");
        setSizeLimitKB(0);
      }
      setBgColor(p.bgDefault ?? "#FFFFFF");
    },
    [],
  );

  /** 换底色：立即生效；未抠图时提示需先 AI 生成 */
  const handleBgColor = useCallback(
    (hex: string) => {
      setBgColor(hex);
      setKeepBg(false);
      if (srcImg && !cutImg && !keepBg && !aiBusy) {
        setStatusOk(t("idphoto.status.needAiFirst"));
      }
    },
    [srcImg, cutImg, keepBg, aiBusy, t, setStatusOk],
  );
```

5. import 行补充 `DIGITAL`：把 specs 的 import 改为

```tsx
import { DIGITAL, SIZE_PRESETS, currentSize, headRange, headTarget } from "~/lib/idphoto/specs";
```

6. 初始 state 同步默认规格的底色与数字预设：把 `const [bgColor, setBgColor] = useState("#FFFFFF");` 改为

```tsx
const [bgColor, setBgColor] = useState(SIZE_PRESETS[0].bgDefault ?? "#FFFFFF");
```

- [ ] **Step 7: 验证**

Run: `npx playwright test tests/idphoto.spec.ts && npx tsc --noEmit`
Expected: PASS / 无新错误

手测追加（dev server）：规格卡片点击切换 → mm 信息随之变化（一寸显示 `295×413` 与 `约 25.0×35.0 mm`）；选美国 2×2 → 出现背景合规提示、数字提交预设自动变为 DS-160。

在 `tests/idphoto.spec.ts` 追加断言用例：

```ts
test("规格选择联动", async ({ page }) => {
  await page.goto("/idphoto");
  await expect(page.getByText("295×413px").first()).toBeVisible();
});
```

- [ ] **Step 8: Commit**

```bash
git add app/components/idphoto tests/idphoto.spec.ts
git commit -m "feat(idphoto): 规格选择与底色选择"
```

---

### Task 7: AI 生成流程 + 合规检测 + 手动微调

**Files:**
- Create: `app/components/idphoto/GeneratePanel.tsx`
- Create: `app/components/idphoto/CompliancePanel.tsx`
- Create: `app/components/idphoto/AdjustPanel.tsx`
- Modify: `app/components/idphoto/IdPhotoWorkbench.tsx`

- [ ] **Step 1: 创建 GeneratePanel.tsx**

```tsx
import { useTranslation } from "react-i18next";
import { Sparkles } from "lucide-react";
import { Button } from "~/components/ui/button";

interface GeneratePanelProps {
  disabled: boolean;
  busy: boolean;
  keepBg: boolean;
  onAI: () => void;
  onCropOnly: () => void;
}

export function GeneratePanel({ disabled, busy, keepBg, onAI, onCropOnly }: GeneratePanelProps) {
  const { t } = useTranslation();
  return (
    <div>
      <Button className="w-full" disabled={disabled || busy} onClick={onAI}>
        <Sparkles className="size-4" />
        {busy ? t("idphoto.status.runtimeLoading") : t("idphoto.btn.ai")}
      </Button>
      <p className="mt-2 text-[11px] leading-relaxed text-muted-foreground">{t("idphoto.ai.hint")}</p>
      {!keepBg && (
        <Button variant="secondary" className="mt-2 w-full" disabled={disabled || busy} onClick={onCropOnly}>
          {t("idphoto.btn.cropOnly")}
        </Button>
      )}
    </div>
  );
}
```

- [ ] **Step 2: 创建 CompliancePanel.tsx**

```tsx
import { useTranslation } from "react-i18next";

interface CompliancePanelProps {
  ready: boolean;
  /** 头占比百分数；null 表示暂不可测 */
  ratioPct: number | null;
  rangePct: [number, number];
  showRuler: boolean;
  onToggleRuler: (v: boolean) => void;
}

export function CompliancePanel({ ready, ratioPct, rangePct, showRuler, onToggleRuler }: CompliancePanelProps) {
  const { t } = useTranslation();
  const state =
    !ready || ratioPct == null ? "unknown" : ratioPct >= rangePct[0] - 0.5 && ratioPct <= rangePct[1] + 0.5 ? "ok" : "bad";
  const headText =
    state === "unknown"
      ? t("idphoto.compliance.unknown")
      : state === "ok"
        ? t("idphoto.compliance.ok", { pct: ratioPct, lo: rangePct[0], hi: rangePct[1] })
        : t("idphoto.compliance.bad", { pct: ratioPct, lo: rangePct[0], hi: rangePct[1] });

  return (
    <div>
      <div
        className={
          "rounded-lg px-3 py-2 text-[13px] font-bold leading-snug " +
          (state === "ok"
            ? "bg-emerald-500/10 text-emerald-600 dark:text-emerald-400"
            : state === "bad"
              ? "bg-red-500/10 text-red-600 dark:text-red-400"
              : "bg-muted text-muted-foreground")
        }
      >
        {headText}
      </div>
      <label className="mt-2 flex cursor-pointer items-center gap-1.5 text-xs text-foreground">
        <input
          type="checkbox"
          checked={showRuler}
          onChange={(e) => onToggleRuler(e.target.checked)}
          className="accent-[#006c49]"
        />
        {t("idphoto.ruler.show")}
      </label>
      <ul className="mt-2 space-y-1">
        {[1, 2, 3, 4, 5].map((i) => (
          <li key={i} className="flex gap-1.5 text-xs leading-relaxed text-muted-foreground">
            <span aria-hidden>•</span>
            <span>{t(`idphoto.tip.${i}`)}</span>
          </li>
        ))}
      </ul>
    </div>
  );
}
```

- [ ] **Step 3: 创建 AdjustPanel.tsx**

```tsx
import { useTranslation } from "react-i18next";
import { Button } from "~/components/ui/button";

export interface Adjust {
  zoom: number; // 50..250
  x: number; // -100..100
  y: number;
}

interface AdjustPanelProps {
  adjust: Adjust;
  onChange: (a: Adjust) => void;
  onReset: () => void;
}

function Row({ label, min, max, value, onChange }: { label: string; min: number; max: number; value: number; onChange: (v: number) => void }) {
  return (
    <div className="mb-1.5 flex items-center gap-2">
      <label className="w-12 shrink-0 text-xs text-muted-foreground">{label}</label>
      <input
        type="range"
        min={min}
        max={max}
        value={value}
        onChange={(e) => onChange(+e.target.value)}
        className="h-1.5 flex-1 accent-[#006c49]"
      />
      <span className="w-10 shrink-0 text-right text-xs tabular-nums text-muted-foreground">{value}</span>
    </div>
  );
}

export function AdjustPanel({ adjust, onChange, onReset }: AdjustPanelProps) {
  const { t } = useTranslation();
  return (
    <div>
      <Row label={t("idphoto.adj.zoom")} min={50} max={250} value={adjust.zoom} onChange={(v) => onChange({ ...adjust, zoom: v })} />
      <Row label={t("idphoto.adj.x")} min={-100} max={100} value={adjust.x} onChange={(v) => onChange({ ...adjust, x: v })} />
      <Row label={t("idphoto.adj.y")} min={-100} max={100} value={adjust.y} onChange={(v) => onChange({ ...adjust, y: v })} />
      <Button variant="secondary" className="mt-2 w-full" onClick={onReset}>
        {t("idphoto.adj.reset")}
      </Button>
    </div>
  );
}
```

- [ ] **Step 4: Workbench 接入 runAI 与三个面板**

Modify `IdPhotoWorkbench.tsx`：

1. import 增加：

```tsx
import { GeneratePanel } from "./GeneratePanel";
import { CompliancePanel } from "./CompliancePanel";
import { AdjustPanel } from "./AdjustPanel";
```

2. 把注释 `// TASK7: runAI 在此追加` 替换为：

```tsx
  const runAI = useCallback(async () => {
    if (!srcImg || aiBusy) return;
    setAiBusy(true);
    try {
      setStatusOk(t("idphoto.status.faceModel"));
      const fb = await detectFace(srcImg);
      setFaceBox(fb);
      if (!keepBg) {
        const seg = await segmentImage(srcImg, setSegEvent);
        setCutImg(seg.cut);
        setPersonTop(seg.personTopSrc);
      } else {
        setCutImg(null);
        setPersonTop(null);
      }
      resetAdjust();
      setResultReady(true);
      setStatusOk(t("idphoto.status.done"));
    } catch (e) {
      console.error(e);
      const msg = e instanceof Error ? e.message : String(e);
      const isNet = /fetch|network|网络/i.test(msg);
      const parts = [t("idphoto.ai.failPrefix") + msg];
      if (isNet) parts.push(t("idphoto.ai.netHint"));
      parts.push(t("idphoto.ai.offlineHint"));
      setStatusErr(parts.join("\n"));
    } finally {
      setAiBusy(false);
    }
  }, [srcImg, aiBusy, keepBg, t, resetAdjust, setStatusOk, setStatusErr]);
```

3. `{/* TASK7: 「一键 AI 生成」按钮…替换本节内容 */}` 所在的整个 step4 section 替换为：

```tsx
        <section className="rounded-2xl border border-border bg-card p-4">
          <StepTitle n={4}>{t("idphoto.step.ai")}</StepTitle>
          <GeneratePanel
            disabled={!srcImg}
            busy={aiBusy}
            keepBg={keepBg}
            onAI={runAI}
            onCropOnly={runCropOnly}
          />
        </section>
```

（原 Task 5 版本里的独立「仅裁剪排版」Button 随整节一起被替换。）

4. `{/* TASK7: 合规检测 CompliancePanel */}` 替换为：

```tsx
        <section className="rounded-2xl border border-border bg-card p-4">
          <StepTitle n={5}>{t("idphoto.step.compliance")}</StepTitle>
          <CompliancePanel
            ready={resultReady && headRatioPct != null}
            ratioPct={headRatioPct}
            rangePct={[Math.round(loRatio * 100), Math.round(hiRatio * 100)]}
            showRuler={showRuler}
            onToggleRuler={setShowRuler}
          />
        </section>
```

5. `{/* TASK7: 微调 AdjustPanel */}` 替换为：

```tsx
        <section className="rounded-2xl border border-border bg-card p-4">
          <StepTitle n={6}>{t("idphoto.step.adjust")}</StepTitle>
          <AdjustPanel adjust={adjust} onChange={setAdjust} onReset={resetAdjust} />
        </section>
```

6. 右侧预览区底部说明文字 `<p>{t("idphoto.ruler.show")}</p>` 删除（开关已入第 5 步面板）。

- [ ] **Step 5: 验证**

Run: `npx playwright test tests/idphoto.spec.ts && npx tsc --noEmit`
Expected: PASS / 无新错误

手测追加：上传正面人像 → 点「一键 AI 生成」→ 状态行依次出现下载百分比/编译/抠图提示 → 完成后预览为换底证件照，标尺线与合规 badge 出现；拖动缩放滑条，绿色/红色状态随头身比变化。

- [ ] **Step 6: Commit**

```bash
git add app/components/idphoto
git commit -m "feat(idphoto): AI 生成、合规检测与手动微调"
```

---

### Task 8: 导出面板

**Files:**
- Create: `app/components/idphoto/ExportPanel.tsx`
- Modify: `app/components/idphoto/IdPhotoWorkbench.tsx`

- [ ] **Step 1: 创建 ExportPanel.tsx**

```tsx
import { useTranslation } from "react-i18next";
import { Download } from "lucide-react";
import { Button } from "~/components/ui/button";
import { DIGITAL, type DigitalKey } from "~/lib/idphoto/specs";

interface ExportPanelProps {
  digitalKey: string;
  sizeLimitKB: number;
  format: "jpeg" | "png";
  note: string;
  disabled: boolean;
  onDigital: (v: string) => void;
  onSizeLimit: (v: number) => void;
  onFormat: (v: "jpeg" | "png") => void;
  onExport: () => void;
}

const DIGITAL_KEYS = Object.keys(DIGITAL) as DigitalKey[];

export function ExportPanel({ digitalKey, sizeLimitKB, format, note, disabled, onDigital, onSizeLimit, onFormat, onExport }: ExportPanelProps) {
  const { t } = useTranslation();
  return (
    <div>
      <div className="mb-2 flex items-center gap-2">
        <label className="w-16 shrink-0 text-xs text-muted-foreground">{t("idphoto.digital.label")}</label>
        <select
          value={digitalKey}
          onChange={(e) => onDigital(e.target.value)}
          className="min-w-0 flex-1 rounded-lg border border-input bg-background px-2 py-1.5 text-sm"
        >
          <option value="">{t("idphoto.digital.none")}</option>
          {DIGITAL_KEYS.map((k) => (
            <option key={k} value={k}>
              {t(`idphoto.digital.${k}`)}
            </option>
          ))}
        </select>
      </div>
      <div className="mb-3 flex items-center gap-2">
        <select
          value={format}
          onChange={(e) => onFormat(e.target.value as "jpeg" | "png")}
          aria-label={t("idphoto.export.format")}
          className="rounded-lg border border-input bg-background px-2 py-1.5 text-sm"
        >
          <option value="jpeg">{t("idphoto.export.jpg")}</option>
          <option value="png">{t("idphoto.export.png")}</option>
        </select>
        <label className="shrink-0 text-xs text-muted-foreground">{t("idphoto.export.sizeLimit")}</label>
        <input
          type="number"
          min={0}
          value={sizeLimitKB}
          onChange={(e) => onSizeLimit(Math.max(0, +e.target.value || 0))}
          className="w-20 rounded-lg border border-input bg-background px-2 py-1.5 text-sm"
        />
        <span className="text-[11px] text-muted-foreground">{t("idphoto.export.kbUnit")}</span>
      </div>
      {digitalKey && (
        <div className="mb-3 rounded-lg bg-muted px-3 py-2 text-xs leading-relaxed text-muted-foreground">
          {t(`idphoto.digital.note.${digitalKey}`)}
        </div>
      )}
      <Button className="w-full" disabled={disabled} onClick={onExport}>
        <Download className="size-4" />
        {t("idphoto.export.btn")}
      </Button>
      {note && <div className="mt-2 rounded-lg bg-muted px-3 py-2 text-xs text-muted-foreground">{note}</div>}
    </div>
  );
}
```

- [ ] **Step 2: Workbench 接入导出**

Modify `IdPhotoWorkbench.tsx`：

1. import 增加：

```tsx
import { ExportPanel } from "./ExportPanel";
import { canvasToBlobLimit, downloadBlob } from "~/lib/idphoto/exportImage";
```

2. 状态区新增：

```tsx
const [exportFormat, setExportFormat] = useState<"jpeg" | "png">("jpeg");
const [exportNote, setExportNote] = useState("");
```

3. 行为区新增（`runAI` 之后）：

```tsx
  const runExport = useCallback(async () => {
    if (!resultReady) return;
    const canvas = resultRef.current;
    if (!canvas) return;
    const s = effectiveSize;
    const { blob, note } = await canvasToBlobLimit(canvas, exportFormat, sizeLimitKB);
    const ext = exportFormat === "png" ? "png" : "jpg";
    downloadBlob(blob, `${t("idphoto.file.photo")}_${t(`idphoto.spec.${s.key}.name`)}_${s.w}x${s.h}.${ext}`);
    setExportNote(t("idphoto.export.done", { info: `${s.w}×${s.h}px ${note}` }));
  }, [resultReady, effectiveSize, exportFormat, sizeLimitKB, t]);
```

4. 数字提交预设联动（规格切换时已由 selectSpec 设置；此处处理用户手动切换）——在 ExportPanel 的 onDigital 里实现，见下一步 JSX。

5. `{/* TASK8: 导出 ExportPanel */}` 锚点替换为：

```tsx
        <section className="rounded-2xl border border-border bg-card p-4">
          <StepTitle n={7}>{t("idphoto.step.export")}</StepTitle>
          <ExportPanel
            digitalKey={digitalKey}
            sizeLimitKB={sizeLimitKB}
            format={exportFormat}
            note={exportNote}
            disabled={!resultReady || aiBusy}
            onDigital={(v) => {
              setDigitalKey(v);
              setSizeLimitKB(v && DIGITAL[v as keyof typeof DIGITAL] ? DIGITAL[v as keyof typeof DIGITAL].maxKB : 0);
            }}
            onSizeLimit={setSizeLimitKB}
            onFormat={setExportFormat}
            onExport={runExport}
          />
        </section>
```

- [ ] **Step 3: 验证**

Run: `npx playwright test tests/idphoto.spec.ts && npx tsc --noEmit`
Expected: PASS / 无新错误

手测追加：生成后选 JPG + 上限 240KB → 导出 → 文件名含规格名，状态 note 显示质量百分比与实际 KB；PNG 导出不受上限影响。

- [ ] **Step 4: Commit**

```bash
git add app/components/idphoto
git commit -m "feat(idphoto): 证件照导出与数字提交预设"
```

---

### Task 9: 相纸排版 Tab

**Files:**
- Create: `app/components/idphoto/PrintLayoutPanel.tsx`
- Modify: `app/components/idphoto/IdPhotoWorkbench.tsx`

- [ ] **Step 1: 创建 PrintLayoutPanel.tsx**

```tsx
import { useRef, useState } from "react";
import { useTranslation } from "react-i18next";
import { Button } from "~/components/ui/button";
import { PAPERS, mm2px, type EffectiveSize } from "~/lib/idphoto/specs";
import { buildPrintLayout, drawPrintLayout, downloadBlob, type PrintLayout } from "~/lib/idphoto/exportImage";

interface PrintLayoutPanelProps {
  /** 结果画布引用（排版数据源） */
  sourceRef: React.RefObject<HTMLCanvasElement | null>;
  size: EffectiveSize;
  resultReady: boolean;
}

type Orient = "portrait" | "landscape";

export function PrintLayoutPanel({ sourceRef, size, resultReady }: PrintLayoutPanelProps) {
  const { t } = useTranslation();
  const printRef = useRef<HTMLCanvasElement>(null);
  const [paper, setPaper] = useState("A4");
  const [paperOrient, setPaperOrient] = useState<Orient>("portrait");
  const [photoOrient, setPhotoOrient] = useState<Orient>("portrait");
  const [info, setInfo] = useState<{ kind: "initial" | "error" | "summary"; text: string }>({ kind: "initial", text: "" });
  const [layout, setLayout] = useState<PrintLayout | null>(null);

  const runLayout = () => {
    if (!resultReady) {
      setInfo({ kind: "error", text: t("idphoto.print.needGenerate") });
      return;
    }
    const pw = PAPERS[paper] ?? PAPERS.A4;
    const [pwmm, phmm]: [number, number] = paperOrient === "landscape" ? [pw.hmm, pw.wmm] : [pw.wmm, pw.hmm];
    const [fwmm, fhmm]: [number, number] = photoOrient === "landscape" ? [size.hmm, size.wmm] : [size.wmm, size.hmm];
    const l = buildPrintLayout(pwmm, phmm, fwmm, fhmm);
    if (!l) {
      setInfo({ kind: "error", text: t("idphoto.print.fitError") });
      setLayout(null);
      return;
    }
    const canvas = printRef.current;
    const src = sourceRef.current;
    if (!canvas || !src) return;
    drawPrintLayout(canvas, src, l, photoOrient === "landscape");
    setLayout(l);
    setInfo({
      kind: "summary",
      text: t("idphoto.print.summary", {
        paper: `${t(`idphoto.paper.${paper}`)}·${t(`idphoto.orient.${paperOrient}`)}`,
        fw: fwmm.toFixed(1),
        fh: fhmm.toFixed(1),
        total: l.total,
        cols: l.cols,
        rows: l.rows,
        w: mm2px(pwmm),
        h: mm2px(phmm),
      }),
    });
  };

  const runExport = () => {
    const canvas = printRef.current;
    if (!canvas) return;
    canvas.toBlob(
      (b) => b && downloadBlob(b, `${t("idphoto.file.print")}_${paper}_${paperOrient === "landscape" ? "H" : "V"}.jpg`),
      "image/jpeg",
      0.95,
    );
  };

  const selectCls = "rounded-lg border border-input bg-background px-2 py-1.5 text-sm";

  return (
    <div className="flex flex-col items-center">
      <div className="mb-3 flex flex-wrap items-end justify-center gap-2">
        <Field label={t("idphoto.paper.label")}>
          <select value={paper} onChange={(e) => setPaper(e.target.value)} className={selectCls}>
            {Object.keys(PAPERS).map((k) => (
              <option key={k} value={k}>
                {t(`idphoto.paper.${k}`)}
              </option>
            ))}
          </select>
        </Field>
        <Field label={t("idphoto.print.paperOrient")}>
          <select value={paperOrient} onChange={(e) => setPaperOrient(e.target.value as Orient)} className={selectCls}>
            <option value="portrait">{t("idphoto.orient.portrait")}</option>
            <option value="landscape">{t("idphoto.orient.landscape")}</option>
          </select>
        </Field>
        <Field label={t("idphoto.print.photoOrient")}>
          <select value={photoOrient} onChange={(e) => setPhotoOrient(e.target.value as Orient)} className={selectCls}>
            <option value="portrait">{t("idphoto.orient.portrait")}</option>
            <option value="landscape">{t("idphoto.orient.landscape")}</option>
          </select>
        </Field>
        <Button onClick={runLayout}>{t("idphoto.print.btnLayout")}</Button>
        <Button variant="secondary" disabled={!layout} onClick={runExport}>
          {t("idphoto.print.btnExportPrint")}
        </Button>
      </div>

      <div
        data-testid="idphoto-print-info"
        className={
          "mb-3 w-full rounded-lg px-3 py-2 text-center text-xs leading-relaxed " +
          (info.kind === "error"
            ? "bg-red-500/10 text-red-600 dark:text-red-400"
            : info.kind === "summary"
              ? "bg-muted text-foreground"
              : "bg-muted/60 text-muted-foreground")
        }
      >
        {info.text || t("idphoto.print.initial")}
      </div>

      <canvas ref={printRef} width={600} height={400} className="h-auto max-h-[70vh] max-w-full rounded-lg border border-border" />
    </div>
  );
}

function Field({ label, children }: { label: string; children: React.ReactNode }) {
  return (
    <label className="flex flex-col gap-1">
      <span className="text-[11px] text-muted-foreground">{label}</span>
      {children}
    </label>
  );
}
```

- [ ] **Step 2: Workbench 用 Tabs 包右侧预览区**

Modify `IdPhotoWorkbench.tsx`：

1. import 增加：

```tsx
import { Tabs, TabsContent, TabsList, TabsTrigger } from "~/components/ui/tabs";
import { PrintLayoutPanel } from "./PrintLayoutPanel";
```

2. 右侧预览区（`{/* TASK9: Tabs 包裹 … */}` 与 `<PreviewCanvas …/>`、`<p>…ruler.show…</p>`）整体替换为：

```tsx
        <Tabs defaultValue="photo">
          <TabsList className="mx-auto mb-4">
            <TabsTrigger value="photo">{t("idphoto.tab.photo")}</TabsTrigger>
            <TabsTrigger value="print">{t("idphoto.tab.print")}</TabsTrigger>
          </TabsList>
          <TabsContent value="photo" className="mt-0">
            <PreviewCanvas resultRef={resultRef} rulerRef={rulerRef} W={effectiveSize.w} H={effectiveSize.h} />
          </TabsContent>
          <TabsContent value="print" className="mt-0">
            <PrintLayoutPanel sourceRef={resultRef} size={effectiveSize} resultReady={resultReady} />
          </TabsContent>
        </Tabs>
```

3. 删除 Task 5 版本里残留的 `{/* TASK9 */}` 锚点注释。

- [ ] **Step 3: 验证**

Run: `npx playwright test tests/idphoto.spec.ts && npx tsc --noEmit`
Expected: PASS / 无新错误

手测追加：生成证件照 → 切「相纸排版打印」→ A4 竖向生成排版（一寸应排 ~4列×7行量级，具体以 mm 计算）→ 导出 JPG。

在 `tests/idphoto.spec.ts` 追加：

```ts
test("排版 tab 可切换", async ({ page }) => {
  await page.goto("/idphoto");
  await page.getByRole("tab", { name: /相纸排版|Print Layout|impression|impresión|impressão/i }).click();
  await expect(page.getByTestId("idphoto-print-info")).toBeVisible();
});
```

- [ ] **Step 4: Commit**

```bash
git add app/components/idphoto tests/idphoto.spec.ts
git commit -m "feat(idphoto): 相纸排版打印 tab"
```

---

### Task 10: 全站集成点

**Files:**
- Modify: `app/components/HomeHeader.tsx`
- Modify: `app/routes/freetool.tsx`
- Modify: `app/lib/seo.ts`
- Modify: `server/api.ts`（STATIC_PAGES 约 L182–196）
- Test: `tests/idphoto.spec.ts`

- [ ] **Step 1: HomeHeader 三处接入**

1. `isActive` 判定（约 L67-72）末尾追加一行：

```tsx
    location.pathname.startsWith("/idphoto") ||
```

2. 桌面下拉菜单：`</DropdownLink>` of `/any2md`（约 L113-115）之后追加：

```tsx
          <DropdownLink href="/idphoto" onClick={() => setOpen(false)}>
            {t("nav.idphoto")}
          </DropdownLink>
```

3. 移动端两处工具列表（约 L288 与 L432 的 any2md 行之后）各追加：

```tsx
              <MobileNavLink href="/idphoto" onClick={() => setMobileNavOpen(false)}>{t("nav.idphoto")}</MobileNavLink>
```

（缩进对齐相邻行；两处分别处于不同嵌套层级，照抄各自邻居的缩进。）

- [ ] **Step 2: freetool 卡片 + JSON-LD**

Modify `app/routes/freetool.tsx`：

1. lucide import 行加入 `Camera`：

```tsx
import { Timer, Music, FileText, FileCode2, Camera, ArrowRight, Sparkles } from "lucide-react";
```

2. JSON-LD `itemListElement` 追加第 5 项（any2md 条目之后）：

```tsx
              {
                "@type": "ListItem",
                position: 5,
                name: i18n.t("freetool.item.idphoto.title"),
                url: "https://100mini.com/idphoto",
              },
```

3. `tools` 数组 any2md 条目之后追加：

```tsx
    {
      href: "/idphoto" as const,
      icon: Camera,
      title: t("freetool.item.idphoto.title"),
      desc: t("freetool.item.idphoto.desc"),
    },
```

- [ ] **Step 3: seo.ts pageKeys**

在 `case "/freetool":` 之后追加：

```ts
    case "/idphoto":
      return { name: "idphoto.heading", desc: "idphoto.desc" };
```

- [ ] **Step 4: sitemap**

Modify `server/api.ts` STATIC_PAGES，在 `/freetool` 条目之后插入：

```ts
  { loc: "/idphoto", changefreq: "weekly", priority: "0.7" },
```

- [ ] **Step 5: 测试 + 验证**

`tests/idphoto.spec.ts` 追加：

```ts
test("freetool 列表包含证件照入口", async ({ page }) => {
  await page.goto("/freetool");
  await expect(
    page.getByRole("link").filter({ hasText: /AI 证件照工具|AI ID Photo Tool|identité|identificación|identificação/ }).first(),
  ).toBeVisible();
});
```

Run: `npx playwright test tests/idphoto.spec.ts && npx tsc --noEmit && npm run build`
Expected: 全部 PASS；构建成功（确认无 CDN URL 进 bundle 报错）

再手验 SEO 输出：

```bash
npm run dev & sleep 5
curl -s http://localhost:5173/sitemap-en.xml | grep idphoto
curl -s http://localhost:5173/en/idphoto | grep -o '<title>[^<]*</title>'
kill %1
```

Expected: sitemap 含 `/idphoto`；页面 title 为英文版 idphoto.title。

- [ ] **Step 6: Commit**

```bash
git add app/components/HomeHeader.tsx app/routes/freetool.tsx app/lib/seo.ts server/api.ts tests/idphoto.spec.ts
git commit -m "feat(idphoto): 导航/freetool/SEO/sitemap 全站接入"
```

---

### Task 11: 全量验证与收尾

- [ ] **Step 1: 构建 + 全部测试**

Run: `npm run build && npx playwright test`
Expected: 构建成功；全部 spec 通过（含既有 pomodoro 等）

- [ ] **Step 2: 手测清单**（dev server）

| # | 项目 | 通过标准 |
|---|---|---|
| 1 | 上传→AI 生成全流程 | 进度文案递进；完成后自动定位 |
| 2 | 换底色即时生效 | 点色板立即变色，无需重新抠图 |
| 3 | 合规标尺 | 缩放滑条拖动时绿↔红切换、标签百分比联动 |
| 4 | 数字提交上限 | DS-160 导出 ≤240KB 或显示最低质量说明 |
| 5 | 相纸排版 | 一寸@A4 正确网格 + 裁剪线；导出可打开 |
| 6 | 仅裁剪排版离线路径 | 断网点「仅裁剪排版」仍可用 |
| 7 | AI 失败降级 | 断网点 AI → 错误文案含 netHint/offlineHint |
| 8 | 语言切换 | zh/en/fr/es/pt 五语抽查 heading/按钮/合规提示 |
| 9 | 暗色模式 | 控制面板、预览棋盘格、状态行无刺眼白块 |
| 10 | 移动端宽度 375px | 两栏变单列，规格网格可滚动 |

- [ ] **Step 3: 收尾提交（如有修复）**

```bash
git add -A
git commit -m "fix(idphoto): 验收修复"
```

---

## Self-Review 记录

- 规格覆盖：spec 文档 §1–§8 ↔ Task 映射：文件结构=T1-T3+T5-T9；布局=Task5/9；状态流=Task5；AI 加载=Task2；错误降级=T5/T7/T9；i18n=Task4；集成点=Task10；测试=各任务+T11。✓ 无缺口。
- 类型一致性：`Box/Base/HeadSrc/SegEvent/EffectiveSize/DigitalKey/Adjust` 各任务签名已交叉核对；`drawRuler` 的 `ComposeGeom` 字段（srcW/srcH/W/H/base/zoom/adjX/adjY）在 Workbench 调用处一致。✓
- 占位符：无 TBD；模板逐行移植部分以「精确行号 + 原样复制」表达，源码在仓库内可达。✓
