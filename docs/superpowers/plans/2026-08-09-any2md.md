# any2md 功能实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 100mini 新增 `/any2md` 页面，用 `@firecrawl/anydoc-wasm` 在浏览器端把任意办公文档转成 Markdown，支持下载/复制/跳转 MD转HTML。

**Architecture:** 三段式页面（Dropzone → 结果栏 → 预览）。WASM 引擎仅在浏览器端动态 import + `init()`，SSR 不加载。转换逻辑封装在 `app/lib/any2md/convert.ts`，UI 拆为 `FileDrop` / `ResultPanel` / `MdView` 三个组件。跳转 MD转HTML 通过 `sessionStorage` 传递草稿。

**Tech Stack:** React 19 / TanStack Start / Tailwind v4 / i18next / `@firecrawl/anydoc-wasm` / `marked` / Playwright

---

## 文件结构

```
app/
  lib/any2md/
    convert.ts            # ensureEngine + toMarkdownFromFile 封装，格式列表/扩展名校验
  components/any2md/
    FileDrop.tsx          # 拖拽/选择上传区（含引擎加载状态、大小/格式校验）
    ResultPanel.tsx       # 结果栏 + 下载/复制/跳转按钮
    MdView.tsx            # marked 渲染预览
  routes/any2md.tsx       # 页面（三段式 + head SEO）
  routes/md2html.tsx      # 修改：挂载时读取 sessionStorage 草稿
  components/HomeHeader.tsx # 修改：ToolsDropdown 加 nav.any2md 链接
  lib/locales/zh.json     # 修改：新增 i18n key
  lib/locales/en.json     # 修改：新增 i18n key
tests/
  any2md.spec.ts          # Playwright 端到端测试
package.json               # 修改：加 @firecrawl/anydoc-wasm 依赖
```

---

## Task 1: 安装依赖

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 安装 `@firecrawl/anydoc-wasm`**

```bash
npm install @firecrawl/anydoc-wasm
```

Expected: 依赖写入 `package.json` dependencies，`node_modules/@firecrawl/anydoc-wasm` 存在。

- [ ] **Step 2: 提交**

```bash
git add package.json package-lock.json
git commit -m "chore(deps): add @firecrawl/anydoc-wasm"
```

---

## Task 2: 转换引擎封装 `app/lib/any2md/convert.ts`

**Files:**
- Create: `app/lib/any2md/convert.ts`

封装 WASM 引擎：幂等 init、格式检测、文件转 Markdown、错误码翻译、支持格式校验。

- [ ] **Step 1: 创建 `convert.ts`**

```ts
export const SUPPORTED_EXTS = [
  "doc", "docx", "ppt", "pptx", "xls", "xlsx",
  "odt", "ods", "odp", "rtf", "epub", "csv", "pdf",
];

export const MAX_SIZE = 5 * 1024 * 1024;

export type EngineStatus = "idle" | "loading" | "ready" | "error";

type WasmModule = typeof import("@firecrawl/anydoc-wasm");

let engine: WasmModule | null = null;
let enginePromise: Promise<WasmModule> | null = null;

export function ensureEngine(): Promise<WasmModule> {
  if (engine) return Promise.resolve(engine);
  if (!enginePromise) {
    enginePromise = (async () => {
      const mod = (await import("@firecrawl/anydoc-wasm")) as WasmModule;
      await mod.default();
      engine = mod;
      return mod;
    })();
  }
  return enginePromise;
}

export function isSupportedExt(name: string): boolean {
  const ext = name.split(".").pop()?.toLowerCase() ?? "";
  return SUPPORTED_EXTS.includes(ext);
}

export function formatLabel(format: string | undefined): string {
  return format ?? "unknown";
}

export interface ConvertResult {
  markdown: string;
  format: string | undefined;
  ms: number;
}

export function toMarkdownFromFile(
  bytes: Uint8Array,
  name: string,
): ConvertResult {
  const mod = engine;
  if (!mod) throw new Error("engine-not-ready");
  const format = mod.formatFromBytes(bytes) ?? mod.formatFromPath(name);
  const started = performance.now();
  const markdown = mod.toMarkdownBytes(bytes, format ?? null);
  const ms = Math.max(1, Math.round(performance.now() - started));
  return { markdown, format, ms };
}

const ERROR_KEYS: Record<string, string> = {
  encrypted: "any2md.error.encrypted",
  unsupported: "any2md.error.unsupported",
  malformed: "any2md.error.malformed",
  resourceLimit: "any2md.error.resourceLimit",
  missingPart: "any2md.error.missingPart",
};

export function errorKey(code: unknown): string {
  if (typeof code === "string" && ERROR_KEYS[code]) return ERROR_KEYS[code];
  return "any2md.error.unknown";
}
```

- [ ] **Step 2: typecheck**

Run: `npx tsc --noEmit -p tsconfig.json`
Expected: 无输出（通过）。

- [ ] **Step 3: 提交**

```bash
git add app/lib/any2md/convert.ts
git commit -m "feat(any2md): add wasm conversion engine wrapper"
```

---

## Task 3: i18n 文案

**Files:**
- Modify: `app/lib/locales/zh.json`
- Modify: `app/lib/locales/en.json`

- [ ] **Step 1: zh.json 新增 key**

在 `"nav.md2html": "MD转HTML",` 行后插入一行 `"nav.any2md": "Any转MD",`（位于 `"nav.tools"` 之前）。然后在文件最后一个 key 行 `"nav.bookmark": "收藏此页"` 后追加以下 keys（保持标准 JSON：前一个 key 的 `}` 前逗号正确衔接，最后一个 key 不加逗号）：

```json
  ,"any2md.title": "Any转MD - 任意文档转 Markdown | 100mini"
  ,"any2md.subtitle": "Word、PPT、Excel、PDF 等文档一键转 Markdown，浏览器本地转换，文件不出本机。"
  ,"any2md.drop": "拖入或点击选择文档"
  ,"any2md.dropHint": "支持 doc/docx/ppt/pptx/xls/xlsx/odt/ods/odp/rtf/epub/csv/pdf，≤5MB"
  ,"any2md.loading": "正在加载转换引擎…"
  ,"any2md.loadFailed": "转换引擎加载失败"
  ,"any2md.retry": "重试"
  ,"any2md.sizeError": "文件超过 5MB 限制"
  ,"any2md.typeError": "不支持的文件格式"
  ,"any2md.converting": "正在转换…"
  ,"any2md.resultStats": "{{chars}} 字符 · {{ms}} ms"
  ,"any2md.download": "下载 .md"
  ,"any2md.copy": "复制"
  ,"any2md.copied": "已复制"
  ,"any2md.continueToMd2html": "继续 MD转HTML"
  ,"any2md.reset": "重新选择"
  ,"any2md.error.encrypted": "文件已加密或受密码保护"
  ,"any2md.error.unsupported": "不支持的文件格式，或该 PDF 无可提取文本"
  ,"any2md.error.malformed": "文件结构损坏，无法提取内容"
  ,"any2md.error.resourceLimit": "文件过大或过于复杂，超出转换限制"
  ,"any2md.error.missingPart": "文件不完整，缺少必需部件"
  ,"any2md.error.unknown": "转换失败，请重试"
  ,"md2html.importedFromAny2md": "已从 Any转MD 导入"
```

注意：以上是**插入内容的锚点示意**，最终 JSON 必须合法。推荐具体做法：用文本编辑器在 `"nav.bookmark": "收藏此页"` 行改为 `"nav.bookmark": "收藏此页",` 并换行粘贴从 `"any2md.title"` 开始的完整 key 列表（去掉每行前导逗号，改成前一行以逗号结尾的标准 JSON）。Step 3 的 JSON.parse 校验会兜底。

- [ ] **Step 2: en.json 新增 key**

同样方式插入：

```json
"nav.any2md": "Any to MD",
"any2md.title": "Any to MD - Convert documents to Markdown | 100mini",
"any2md.subtitle": "Convert Word, PPT, Excel, PDF and more to Markdown in your browser. Files never leave your machine.",
"any2md.drop": "Drop a document or click to browse",
"any2md.dropHint": "Supports doc/docx/ppt/pptx/xls/xlsx/odt/ods/odp/rtf/epub/csv/pdf, up to 5MB",
"any2md.loading": "Loading the converter…",
"any2md.loadFailed": "Failed to load the converter",
"any2md.retry": "Retry",
"any2md.sizeError": "File exceeds the 5MB limit",
"any2md.typeError": "Unsupported file format",
"any2md.converting": "Converting…",
"any2md.resultStats": "{{chars}} chars · {{ms}} ms",
"any2md.download": "Download .md",
"any2md.copy": "Copy",
"any2md.copied": "Copied",
"any2md.continueToMd2html": "Continue to MD to HTML",
"any2md.reset": "Choose another file",
"any2md.error.encrypted": "The file is encrypted or password protected",
"any2md.error.unsupported": "Unsupported format, or a PDF with no extractable text",
"any2md.error.malformed": "The file is malformed and cannot be converted",
"any2md.error.resourceLimit": "The file is too large or complex to convert",
"any2md.error.missingPart": "The file is incomplete and missing required parts",
"any2md.error.unknown": "Conversion failed, please try again",
"md2html.importedFromAny2md": "Imported from Any to MD",
```

- [ ] **Step 3: 校验 JSON 合法性**

Run: `node -e "JSON.parse(require('fs').readFileSync('app/lib/locales/zh.json','utf8')); JSON.parse(require('fs').readFileSync('app/lib/locales/en.json','utf8')); console.log('OK')"`
Expected: `OK`

- [ ] **Step 4: 提交**

```bash
git add app/lib/locales/zh.json app/lib/locales/en.json
git commit -m "feat(any2md): add i18n keys"
```

---

## Task 4: `FileDrop` 组件

**Files:**
- Create: `app/components/any2md/FileDrop.tsx`

拖拽/选择上传区，复用现有 DropZone 的交互模式，但按 anydoc 格式白名单校验，并展示引擎加载状态。

- [ ] **Step 1: 创建 `FileDrop.tsx`**

```tsx
import { useRef, useState, useCallback } from "react";
import { Upload, Loader2 } from "lucide-react";
import { useTranslation } from "react-i18next";
import { cn } from "~/lib/utils";
import { isSupportedExt, MAX_SIZE } from "~/lib/any2md/convert";

interface FileDropProps {
  engineStatus: "idle" | "loading" | "ready" | "error";
  busy: boolean;
  onFile: (file: File) => void;
  onRetry?: () => void;
  onEngineError?: () => void;
  error: string | null;
}

export function FileDrop({ engineStatus, busy, onFile, onRetry, error }: FileDropProps) {
  const { t } = useTranslation();
  const [dragOver, setDragOver] = useState(false);
  const inputRef = useRef<HTMLInputElement>(null);

  const handleFile = useCallback(
    (f: File) => {
      if (!isSupportedExt(f.name)) {
        onFile(f);
        return;
      }
      if (f.size > MAX_SIZE) {
        alert(t("any2md.sizeError"));
        return;
      }
      onFile(f);
    },
    [onFile, t]
  );

  const handleDrop = useCallback(
    (e: React.DragEvent) => {
      e.preventDefault();
      setDragOver(false);
      const f = e.dataTransfer.files?.[0];
      if (f) handleFile(f);
    },
    [handleFile]
  );

  const handleDragOver = (e: React.DragEvent) => {
    e.preventDefault();
    setDragOver(true);
  };

  const handleDragLeave = () => setDragOver(false);

  const handleInputChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    const f = e.target.files?.[0];
    if (f) handleFile(f);
  };

  return (
    <div>
      <div
        className={cn(
          "flex flex-col items-center justify-center rounded-2xl border-2 border-dashed p-14 text-center cursor-pointer transition-all",
          dragOver
            ? "border-[#006c49] bg-[#006c49]/5 dark:border-[#4edea3] dark:bg-[#4edea3]/5"
            : "border-[#bbcabf]/40 bg-[#e5eeff]/30 dark:border-[#3c4a42] dark:bg-[#1e314a]/30 hover:border-[#bbcabf]/60"
        )}
        onDrop={handleDrop}
        onDragOver={handleDragOver}
        onDragLeave={handleDragLeave}
        onClick={() => inputRef.current?.click()}
      >
        <input
          ref={inputRef}
          type="file"
          accept={[
            ".doc", ".docx", ".ppt", ".pptx", ".xls", ".xlsx",
            ".odt", ".ods", ".odp", ".rtf", ".epub", ".csv", ".pdf",
          ].join(",")}
          className="hidden"
          onChange={handleInputChange}
        />
        {busy ? (
          <>
            <Loader2 className="size-7 animate-spin text-[#006c49] dark:text-[#4edea3]" />
            <div className="mt-4 text-base text-foreground">{t("any2md.converting")}</div>
          </>
        ) : engineStatus === "loading" ? (
          <>
            <Loader2 className="size-7 animate-spin text-[#006c49] dark:text-[#4edea3]" />
            <div className="mt-4 text-base text-foreground">{t("any2md.loading")}</div>
          </>
        ) : engineStatus === "error" ? (
          <>
            <div className="text-base text-foreground">{t("any2md.loadFailed")}</div>
            <button
              type="button"
              className="mt-3 rounded-lg border border-border px-4 py-1.5 text-sm text-muted-foreground hover:text-foreground"
              onClick={(e) => { e.stopPropagation(); onRetry?.(); }}
            >
              {t("any2md.retry")}
            </button>
          </>
        ) : (
          <>
            <div className="flex size-14 items-center justify-center rounded-2xl bg-[#006c49]/10 text-[#006c49] dark:bg-[#4edea3]/10 dark:text-[#4edea3]">
              <Upload className="size-7" />
            </div>
            <div className="mt-4 text-base text-foreground mb-1">{t("any2md.drop")}</div>
            <div className="text-sm text-muted-foreground">{t("any2md.dropHint")}</div>
          </>
        )}
      </div>
      {error && (
        <div className="mt-3 rounded-lg border border-destructive/20 bg-destructive/10 px-4 py-2 text-sm text-destructive">
          {error}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: typecheck**

Run: `npx tsc --noEmit -p tsconfig.json`
Expected: 无输出。

- [ ] **Step 3: 提交**

```bash
git add app/components/any2md/FileDrop.tsx
git commit -m "feat(any2md): add FileDrop component"
```

---

## Task 5: `MdView` 组件

**Files:**
- Create: `app/components/any2md/MdView.tsx`

用 `marked` 把 Markdown 渲染成 HTML 展示（不套 iframe，直接注入容器，无脚本执行风险——marked 产物是静态 HTML）。

- [ ] **Step 1: 创建 `MdView.tsx`**

```tsx
import { useEffect, useState } from "react";
import { marked } from "marked";

interface MdViewProps {
  markdown: string;
  className?: string;
}

export function MdView({ markdown, className = "" }: MdViewProps) {
  const [html, setHtml] = useState("");

  useEffect(() => {
    let cancelled = false;
    marked
      .parse(markdown, { async: false })
      .then((out) => {
        if (!cancelled) setHtml(String(out));
      })
      .catch(() => {
        if (!cancelled) setHtml(`<pre>${markdown.replace(/</g, "&lt;")}</pre>`);
      });
    return () => {
      cancelled = true;
    };
  }, [markdown]);

  if (!markdown) return null;

  return (
    <div
      className={`prose prose-sm max-w-none overflow-x-auto rounded-xl border border-border bg-background p-6 dark:prose-invert ${className}`}
      dangerouslySetInnerHTML={{ __html: html }}
    />
  );
}
```

- [ ] **Step 2: 确认 `marked` 已在依赖中（md2html 已用，跳过安装）**

Run: `node -e "require('marked')" 2>/dev/null || echo MISSING`
Expected: 无输出（已安装）。

- [ ] **Step 3: typecheck**

Run: `npx tsc --noEmit -p tsconfig.json`
Expected: 无输出。

- [ ] **Step 4: 提交**

```bash
git add app/components/any2md/MdView.tsx
git commit -m "feat(any2md): add Markdown preview component"
```

---

## Task 6: `ResultPanel` 组件

**Files:**
- Create: `app/components/any2md/ResultPanel.tsx`

结果栏：文件名/格式/统计 + 下载/复制/跳转按钮。

- [ ] **Step 1: 创建 `ResultPanel.tsx`**

```tsx
import { useState } from "react";
import { Download, Copy, Check, ArrowRight, RotateCcw } from "lucide-react";
import { useTranslation } from "react-i18next";
import { Button } from "~/components/ui/button";

interface ResultPanelProps {
  fileName: string;
  format: string | undefined;
  chars: number;
  ms: number;
  markdown: string;
  onDownload: () => void;
  onCopy: () => void;
  onContinue: () => void;
  onReset: () => void;
}

export function ResultPanel({
  fileName,
  format,
  chars,
  ms,
  markdown,
  onDownload,
  onCopy,
  onContinue,
  onReset,
}: ResultPanelProps) {
  const { t } = useTranslation();
  const [copied, setCopied] = useState(false);

  const handleCopy = async () => {
    try {
      await navigator.clipboard.writeText(markdown);
      setCopied(true);
      setTimeout(() => setCopied(false), 2000);
    } catch {
      onCopy();
    }
  };

  return (
    <div className="flex flex-wrap items-center gap-3 rounded-xl border border-border bg-white dark:bg-[#15243b] px-4 py-3">
      <div className="min-w-0 flex-1">
        <div className="truncate text-sm font-semibold text-foreground">{fileName}</div>
        <div className="mt-0.5 text-xs text-muted-foreground">
          {format && (
            <span className="mr-2 inline-flex items-center rounded-full bg-[#e5eeff] px-2 py-0.5 font-mono text-[11px] font-medium text-[#0058be] dark:bg-[#1e314a] dark:text-[#adc6ff]">
              {format}
            </span>
          )}
          {t("any2md.resultStats", { chars: chars.toLocaleString(), ms })}
        </div>
      </div>
      <div className="flex items-center gap-2">
        <Button variant="outline" size="sm" className="gap-1.5" onClick={onDownload}>
          <Download className="size-3.5" />
          {t("any2md.download")}
        </Button>
        <Button variant="outline" size="sm" className="gap-1.5" onClick={handleCopy}>
          {copied ? <Check className="size-3.5" /> : <Copy className="size-3.5" />}
          {copied ? t("any2md.copied") : t("any2md.copy")}
        </Button>
        <Button variant="default" size="sm" className="gap-1.5" onClick={onContinue}>
          {t("any2md.continueToMd2html")}
          <ArrowRight className="size-3.5" />
        </Button>
        <Button variant="ghost" size="icon" className="size-8" onClick={onReset} title={t("any2md.reset")}>
          <RotateCcw className="size-4" />
        </Button>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: typecheck**

Run: `npx tsc --noEmit -p tsconfig.json`
Expected: 无输出。

- [ ] **Step 3: 提交**

```bash
git add app/components/any2md/ResultPanel.tsx
git commit -m "feat(any2md): add result panel component"
```

---

## Task 7: 页面路由 `app/routes/any2md.tsx`

**Files:**
- Create: `app/routes/any2md.tsx`

三段式页面：Dropzone → ResultPanel → MdView，管理引擎加载/转换/错误状态。

- [ ] **Step 1: 创建 `any2md.tsx`**

```tsx
import { useState, useEffect, useCallback } from "react";
import { createFileRoute, useNavigate } from "@tanstack/react-router";
import { FileText } from "lucide-react";
import { AppNav } from "~/components/HomeHeader";
import { AppFooter } from "~/components/AppFooter";
import { FileDrop } from "~/components/any2md/FileDrop";
import { ResultPanel } from "~/components/any2md/ResultPanel";
import { MdView } from "~/components/any2md/MdView";
import {
  ensureEngine,
  toMarkdownFromFile,
  isSupportedExt,
  errorKey,
  type EngineStatus,
  type ConvertResult,
} from "~/lib/any2md/convert";
import { useTranslation } from "react-i18next";
import i18n from "~/lib/i18n";

export const Route = createFileRoute("/any2md")({
  head: () => ({
    meta: [
      { title: i18n.t("any2md.title") },
      { name: "description", content: i18n.t("any2md.subtitle") },
      { name: "robots", content: "index, follow" },
      { property: "og:type", content: "website" },
      { property: "og:title", content: i18n.t("any2md.title") },
    ],
  }),
  component: Any2MdPage,
});

function Any2MdPage() {
  const { t } = useTranslation();
  const navigate = useNavigate();
  const [engineStatus, setEngineStatus] = useState<EngineStatus>("idle");
  const [busy, setBusy] = useState(false);
  const [result, setResult] = useState<ConvertResult | null>(null);
  const [fileName, setFileName] = useState("");
  const [error, setError] = useState<string | null>(null);

  const loadEngine = useCallback(() => {
    setEngineStatus("loading");
    ensureEngine()
      .then(() => setEngineStatus("ready"))
      .catch(() => setEngineStatus("error"));
  }, []);

  useEffect(() => {
    loadEngine();
  }, [loadEngine]);

  const handleFile = async (file: File) => {
    setError(null);
    if (!isSupportedExt(file.name)) {
      setError(t("any2md.typeError"));
      return;
    }
    if (file.size > 5 * 1024 * 1024) {
      setError(t("any2md.sizeError"));
      return;
    }
    if (engineStatus !== "ready") {
      setError(t("any2md.loading"));
      return;
    }
    setBusy(true);
    setResult(null);
    setFileName(file.name);
    try {
      const bytes = new Uint8Array(await file.arrayBuffer());
      const res = toMarkdownFromFile(bytes, file.name);
      setResult(res);
    } catch (err) {
      const code = (err as { code?: unknown })?.code;
      setError(t(errorKey(code)));
      setFileName("");
    } finally {
      setBusy(false);
    }
  };

  const handleDownload = () => {
    if (!result) return;
    const base = fileName.replace(/\.[^.]*$/, "") || "document";
    const blob = new Blob([result.markdown], { type: "text/markdown" });
    const url = URL.createObjectURL(blob);
    const a = document.createElement("a");
    a.href = url;
    a.download = `${base}.md`;
    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
    URL.revokeObjectURL(url);
  };

  const handleContinue = () => {
    if (!result) return;
    sessionStorage.setItem("any2md.draft", result.markdown);
    navigate({ to: "/md2html" });
  };

  const handleReset = () => {
    setResult(null);
    setFileName("");
    setError(null);
  };

  return (
    <div className="flex min-h-screen flex-col">
      <AppNav />
      <main className="flex-1 bg-[#eff4ff] dark:bg-[#1e314a]">
        <div className="mx-auto max-w-3xl px-6 pt-10 pb-12">
          <div className="mb-8">
            <div className="flex items-center gap-2">
              <FileText className="size-6 text-[#006c49] dark:text-[#4edea3]" />
              <h1 className="text-2xl font-bold text-foreground">Any 转 MD</h1>
            </div>
            <p className="mt-2 text-sm text-muted-foreground">{t("any2md.subtitle")}</p>
          </div>

          <FileDrop
            engineStatus={engineStatus}
            busy={busy}
            onFile={handleFile}
            onRetry={loadEngine}
            error={error}
          />

          {result && (
            <div className="mt-4">
              <ResultPanel
                fileName={fileName}
                format={result.format}
                chars={result.markdown.length}
                ms={result.ms}
                markdown={result.markdown}
                onDownload={handleDownload}
                onCopy={() => {}}
                onContinue={handleContinue}
                onReset={handleReset}
              />
              <div className="mt-4">
                <MdView markdown={result.markdown} />
              </div>
            </div>
          )}
        </div>
      </main>
      <AppFooter />
    </div>
  );
}
```

- [ ] **Step 2: typecheck**

Run: `npx tsc --noEmit -p tsconfig.json`
Expected: 无输出。

- [ ] **Step 3: 提交**

```bash
git add app/routes/any2md.tsx
git commit -m "feat(any2md): add /any2md page"
```

---

## Task 8: 导航加入口

**Files:**
- Modify: `app/components/HomeHeader.tsx`

- [ ] **Step 1: 在 `ToolsDropdown` 中加链接**

在 `HomeHeader.tsx` 的 `ToolsDropdown` 组件里，`md2html` 的 `DropdownLink` 之后追加：

```tsx
          <DropdownLink href="/any2md" onClick={() => setOpen(false)}>
            {t("nav.any2md")}
          </DropdownLink>
```

- [ ] **Step 2: typecheck**

Run: `npx tsc --noEmit -p tsconfig.json`
Expected: 无输出。

- [ ] **Step 3: 提交**

```bash
git add app/components/HomeHeader.tsx
git commit -m "feat(any2md): add nav entry in tools dropdown"
```

---

## Task 9: md2html 草稿导入

**Files:**
- Modify: `app/routes/md2html.tsx`

- [ ] **Step 1: 添加草稿读取 effect**

在 `Md2HtmlPage` 组件内、现有 `useEffect`（处理 lang 的）之后新增：

```tsx
  const [importToast, setImportToast] = useState(false);

  useEffect(() => {
    const draft = sessionStorage.getItem("any2md.draft");
    if (draft) {
      sessionStorage.removeItem("any2md.draft");
      defaultMdRef.current = draft;
      setMd(draft);
      setImportToast(true);
      setTimeout(() => setImportToast(false), 2500);
    }
  }, []);
```

- [ ] **Step 2: 渲染 toast**

在组件 JSX 根部（`<div className="flex h-screen flex-col overflow-hidden">` 内）追加：

```tsx
      {importToast && (
        <div className="pointer-events-none fixed bottom-6 left-1/2 z-50 -translate-x-1/2 rounded-full border border-border bg-popover px-4 py-2 text-sm shadow-lg">
          {t("md2html.importedFromAny2md")}
        </div>
      )}
```

- [ ] **Step 3: typecheck**

Run: `npx tsc --noEmit -p tsconfig.json`
Expected: 无输出。

- [ ] **Step 4: 提交**

```bash
git add app/routes/md2html.tsx
git commit -m "feat(any2md): import draft into md2html"
```

---

## Task 10: Playwright 端到端测试

**Files:**
- Create: `tests/any2md.spec.ts`

- [ ] **Step 1: 创建测试文件**

```ts
import { test, expect } from "@playwright/test";

test.describe("Any to MD", () => {
  test("should render the page and load the engine", async ({ page }) => {
    await page.goto("/any2md");
    await expect(page.getByText("Any 转 MD")).toBeVisible();
    await expect(page.getByText(/拖入或点击选择文档|正在加载转换引擎/)).toBeVisible({ timeout: 15000 });
  });

  test("should reject unsupported file type", async ({ page }) => {
    await page.goto("/any2md");
    await page.waitForTimeout(3000);
    await page
      .setInputFiles('input[type="file"]', {
        name: "notes.txt",
        mimeType: "text/plain",
        buffer: Buffer.from("hello"),
      });
    await expect(page.getByText("不支持的文件格式")).toBeVisible();
  });

  test("should convert a CSV file to markdown", async ({ page }) => {
    await page.goto("/any2md");
    await page.getByText(/拖入或点击选择文档/).waitFor({ timeout: 20000 });
    await page
      .setInputFiles('input[type="file"]', {
        name: "report.csv",
        mimeType: "text/csv",
        buffer: Buffer.from("name,age\nalice,30\nbob,25\n"),
      });
    await expect(page.getByText(/字符/)).toBeVisible({ timeout: 15000 });
    await expect(page.locator("table")).toBeVisible();
  });
});
```

- [ ] **Step 2: 运行测试**

Run: `npx playwright test tests/any2md.spec.ts`
Expected: 3 个测试通过。若转换/引擎断言受时序影响，调整 waitFor 超时。

- [ ] **Step 3: 提交**

```bash
git add tests/any2md.spec.ts
git commit -m "test(any2md): add e2e tests"
```

---

## Task 11: 构建验证

- [ ] **Step 1: 生产构建**

Run: `npm run build`
Expected: 构建成功。确认 `dist/` 中 anydoc wasm 资源（`anydoc_wasm_bg.wasm` 或 hash 文件名）被正确复制，且 SSR bundle 不包含 wasm 加载失败错误。

注意：若构建报 wasm 相关错误（如 SSR 中 `import("@firecrawl/anydoc-wasm")` 被解析），检查 convert.ts 中动态 import 是否被 SSR 侧执行——ensureEngine 只在客户端 `useEffect` 中调用，SSR 不会触达，应无问题。若 Vite 在客户端打包时无法定位 `new URL(..., import.meta.url)` 的 wasm，检查产物与 `@firecrawl/anydoc-wasm` README 中的 Vite 支持说明。

- [ ] **Step 2: 提交（若有产物修复）**

```bash
git add -A
git commit -m "fix(any2md): build fixes"
```

---

## 自审记录

**Spec 覆盖：**
- 路由/导航：Task 7 + Task 8 ✓
- 三段式布局：Task 4（Dropzone）/ Task 6（结果栏）/ Task 5（预览）✓
- WASM 引擎仅客户端：Task 2 + Task 7 useEffect ✓
- 错误处理 5 code：Task 2 `errorKey` + i18n Task 3 ✓
- 跳转 md2html：Task 7 `handleContinue` + Task 9 草稿导入 ✓
- 文件结构：所有任务文件路径与 spec 第 6 节一致 ✓
- i18n 全 key：Task 3 覆盖 spec 第 7 节清单 ✓
- 测试：Task 10 + Task 11 ✓

**占位符扫描：** 无 TBD/TODO；每个 Step 有完整代码或明确命令。

**类型一致性：** `EngineStatus` / `ConvertResult` / `ensureEngine` / `toMarkdownFromFile` / `errorKey` / `isSupportedExt` 在 Task 2 定义，后续任务引用一致。`FileDrop` props 与页面调用一致（`onRetry` 传 `loadEngine`）。`ResultPanel` props 与页面一致。
